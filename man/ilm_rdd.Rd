% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_rdd.R
\name{ilm_rdd}
\alias{ilm_rdd}
\title{Regression discontinuity}
\usage{
ilm_rdd(
  data,
  y,
  running,
  cutoff = 0,
  treatment = NULL,
  h = NULL,
  kernel = "triangular",
  poly = 1L,
  covariates = NULL,
  family = NULL,
  bw_range = c(0.5, 0.75, 1, 1.5, 2),
  placebo = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{y}{Outcome column.}

\item{running}{Running (forcing) variable.}

\item{cutoff}{Value of \code{running} at which treatment switches on.}

\item{treatment}{Optional column of actual treatment received, used to check
whether assignment is sharp.}

\item{h}{Bandwidth. \code{NULL} uses a rule-of-thumb starting point.}

\item{kernel}{\code{"triangular"} (default), \code{"uniform"} or \code{"epanechnikov"}.}

\item{poly}{Polynomial order on each side; 1 is local linear.}

\item{covariates}{Further columns for the mean structure.}

\item{family}{Response distribution; inferred from \code{y} when \code{NULL}.}

\item{bw_range}{Multipliers of \code{h} for the sensitivity sweep.}

\item{placebo}{Placebo cutoffs, as quantiles of the running variable either
side of the real cutoff. \code{NULL} picks a few.}

\item{verbose}{Narrate each step.}

\item{...}{Passed to \code{\link[=ilm_model]{ilm_model()}}.}
}
\value{
An object of class \code{"ilm_rdd"}: \code{jump}, \code{fit}, \code{bandwidth},
\code{density}, \code{balance}, \code{placebo} and the settings used.
}
\description{
Estimates the jump in an outcome at the point where treatment switches on,
by fitting a straight line on each side of the cutoff within a bandwidth and
taking the difference at the cutoff.
}
\section{What this reports, and what it does not}{


A local linear fit with a triangular kernel, the bandwidth sensitivity laid
out, and the design checks below. It does \strong{not} implement the
bias-corrected robust intervals of Calonico, Cattaneo and Titiunik; the
\code{rdrobust} package does that and does it well. The interval here is the
ordinary one for a weighted local fit, which understates uncertainty when the
bandwidth is large enough for curvature to matter -- which is exactly what
\verb{$bandwidth} is there to show you.

The default is local \strong{linear}. A high-order global polynomial produces
estimates driven by points far from the cutoff and is a well-documented way
to manufacture a discontinuity (Gelman and Imbens 2019). \code{poly} is available
and going above 2 warns.
}

\section{The design checks}{


A regression discontinuity is only as good as the claim that crossing the
cutoff is the \emph{only} thing that changes there.
\itemize{
\item \strong{Density} -- if people can move themselves across the cutoff, the ones
just above are not comparable to the ones just below. The test is whether
the density is \emph{discontinuous} at the cutoff, not whether it is symmetric
about it: a local linear density is fitted on each side and compared there.
Over 500 unmanipulated data sets it flagged 0.046 to 0.060 of them across
uniform, normal and exponential running variables, and caught 0.984 of the
cases where 30\% of the units just below the cutoff had been moved above
it.
\item \strong{Covariate balance} -- anything measured before treatment should not jump
at the cutoff. If it does, something other than treatment changes there.
\item \strong{Placebo cutoffs} -- the same estimate at points where nothing happened.
These should be null; a comparable jump elsewhere means the method is
finding jumps in noise.
\item \strong{Bandwidth sensitivity} -- the estimate across a range of bandwidths. An
effect that appears only in a narrow window is not an effect.
}
}

\section{Fuzzy assignment}{


This estimates a \strong{sharp} design, where crossing the cutoff determines
treatment. If a treatment column is supplied and assignment turns out fuzzy
-- the probability of treatment jumps but not from 0 to 1 -- this says so and
declines to report the sharp estimate as if it were the effect of treatment.
A fuzzy design needs an instrumental-variables estimator, which illume does
not have.
}

\examples{
set.seed(1); n <- 2000
r <- runif(n, -1, 1)
y <- 0.5 * r + 0.8 * (r >= 0) + rnorm(n, 0, 0.5)
ilm_rdd(data.frame(r = r, y = y), "y", "r", cutoff = 0, verbose = FALSE)
}
\references{
Gelman, A. and Imbens, G. (2019). Why high-order polynomials should not be
used in regression discontinuity designs. Journal of Business and Economic
Statistics 37(3).

Calonico, S., Cattaneo, M. D. and Titiunik, R. (2014). Robust nonparametric
confidence intervals for regression-discontinuity designs. Econometrica
82(6).
}
\seealso{
\code{\link[=ilm_plot_rdd]{ilm_plot_rdd()}}, \code{\link[=ilm_did]{ilm_did()}}, \code{\link[=ilm_model]{ilm_model()}}.
}
