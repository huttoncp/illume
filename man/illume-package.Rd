% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/illume-package.R
\docType{package}
\name{illume-package}
\alias{illume}
\alias{illume-package}
\title{illume: Frequentist Regression and Mixed Models with Built-In Diagnostics}
\description{
A frequentist regression workflow aimed at \strong{inference} rather than
prediction. One engine fits gaussian, binomial, Poisson, negative binomial
and multinomial models, so every model returns the same object and shares the
same methods and diagnostics.
}
\section{What problem this solves}{

Fitting a range of models in R usually means a range of packages, each with
its own object, its own \code{anova()} semantics, its own \code{predict()} arguments and
its own idea of what a diagnostic plot is. \code{illume} fits them with one engine,
so the interface does not change as the model gets more complicated -- from
\code{y ~ x} through to a multinomial mixed model with smooths and correlated
errors.

Where an exact small-sample reference exists it is used: a gaussian model
with no random effects reports t and F tests agreeing with \code{\link[stats:lm]{stats::lm()}},
rather than the large-sample approximations the Laplace machinery would
otherwise give.

The \strong{multinomial} family closes a specific gap. \code{\link[nnet:multinom]{nnet::multinom()}} handles
unordered categorical outcomes but has no random effects; \code{\link[lme4:glmer]{lme4::glmer()}} and
\code{glmmTMB} have rich random-effect machinery but no multinomial family; \code{brms}
fits the full model but is far slower and answers a different inferential
question.
}

\section{Families}{

\code{"gaussian"}, \code{"binomial"}, \code{"poisson"}, \code{"nbinom"} and \code{"multinomial"}; see
\code{\link[=lum_family]{lum_family()}}. Zero-inflation, hurdle models and Tweedie are deliberately out
of scope, because \code{glmmTMB} covers those well.
}

\section{How the model is written}{

With \code{J} outcome categories the model has \code{C = J - 1} free linear predictors.
\code{illume} uses \strong{sum-to-zero} coding across categories, so each coefficient is
that category's deviation from the average across categories, \emph{not} a contrast
against a baseline category. This differs from \code{\link[nnet:multinom]{nnet::multinom()}} and \code{brms},
which use baseline-category coding, and matters when reading output: see
\code{\link[=lum_coef_table]{lum_coef_table()}}.
}

\section{Why the diagnostics are prominent}{

This model class fails \emph{quietly}. A fit can return entirely plausible
coefficients while its covariance matrix is unusable, so the standard errors
are meaningless even though nothing looks wrong. Every fit therefore carries a
table of checks (see \code{fit$checks}), each with a status, a plain-language
reason, and a suggested remedy. \code{\link[=summary.lum_model]{summary.lum_model()}} prints them.
}

\section{Main entry points}{

\describe{
\item{\code{\link[=lum_model]{lum_model()}}}{fit a model from a formula, for any family}
\item{\code{\link[=lum_family]{lum_family()}}}{what each response distribution assumes}
\item{\code{\link[=summary.lum_model]{summary.lum_model()}}}{coefficient table plus assumption checks}
\item{\code{\link[=lum_anova]{lum_anova()}}}{Type II/III analysis of deviance for fixed effects}
\item{\code{\link[=predict.lum_model]{predict.lum_model()}}}{category probabilities, conditional or population-averaged}
\item{\code{\link[=lum_appraise]{lum_appraise()}}}{residual diagnostic panels}
\item{\code{\link[=lum_pb_lrt]{lum_pb_lrt()}}}{parametric-bootstrap likelihood-ratio test}
}
}

\references{
Agresti, A. (2013). \emph{Categorical Data Analysis}, 3rd ed. Wiley.
(Chapter 8 covers baseline-category logit models.)

Kristensen, K., Nielsen, A., Berg, C. W., Skaug, H., & Bell, B. M. (2016).
TMB: Automatic differentiation and Laplace approximation.
\emph{Journal of Statistical Software}, 70(5), 1--21. \doi{10.18637/jss.v070.i05}

Bates, D., Maechler, M., Bolker, B., & Walker, S. (2015). Fitting linear
mixed-effects models using lme4. \emph{Journal of Statistical Software}, 67(1),
1--48. \doi{10.18637/jss.v067.i01}

Brooks, M. E., Kristensen, K., van Benthem, K. J., Magnusson, A., Berg, C. W.,
Nielsen, A., Skaug, H. J., Maechler, M., & Bolker, B. M. (2017). glmmTMB
balances speed and flexibility among packages for zero-inflated generalized
linear mixed modeling. \emph{The R Journal}, 9(2), 378--400.

Wood, S. N. (2017). \emph{Generalized Additive Models: An Introduction with R},
2nd ed. Chapman & Hall/CRC.
}
\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/craig-hutton/illume}
  \item Report bugs at \url{https://github.com/craig-hutton/illume/issues}
}

}
\author{
\strong{Maintainer}: Craig Hutton \email{craig.hutton@gmail.com}

}
\keyword{internal}
