% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_check.R
\name{ilm_check_collinearity}
\alias{ilm_check_collinearity}
\title{Variance inflation among fixed effects}
\usage{
ilm_check_collinearity(object, warn = sqrt(5), fail = sqrt(10))
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{warn, fail}{Thresholds on the standard-error inflation factor. The
defaults correspond to the conventional VIF cut-offs of 5 and 10.}
}
\value{
A data frame with one row per fixed-effect term: \code{gvif}, \code{df},
\code{se_inflation}, and a \code{status} of \code{"OK"}, \code{"WARN"} or \code{"FAIL"}.
}
\description{
\code{\link[=ilm_frame_issues]{ilm_frame_issues()}} catches pairs of columns that are nearly identical, but
the case that actually breaks a model is a predictor collinear with a
\emph{combination} of the others, which no pairwise correlation reveals. That is
what variance inflation measures.
}
\details{
For a term with more than one degree of freedom -- a factor, a spline basis
-- the plain VIF is not comparable across terms, so the generalised form
(GVIF) is reported alongside \code{gvif^(1/(2*df))}, which is on the scale of a
standard error inflation and can be compared directly.
}
\examples{
set.seed(1)
d <- ilm_sim()
f <- ilm_model(score ~ income + grp + (1 | id), data = d,
               family = "gaussian", verbose = FALSE)
ilm_check_collinearity(f)
}
\references{
Fox, J. and Monette, G. (1992). Generalized collinearity diagnostics.
Journal of the American Statistical Association, 87(417), 178-183.
}
\seealso{
\code{\link[=ilm_frame_issues]{ilm_frame_issues()}} for collinearity in the raw data.
}
