% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_emmeans.R
\name{ilm_emmeans}
\alias{ilm_emmeans}
\title{Estimated marginal means}
\usage{
ilm_emmeans(
  object,
  specs,
  at = NULL,
  weights = c("equal", "proportional", "cells"),
  type = c("link", "response"),
  level = 0.95
)
}
\arguments{
\item{object}{A fitted \code{\link[=ilm_model]{ilm_model()}}.}

\item{specs}{Variables to keep, as a character vector. Everything else is
averaged over or held at its mean.}

\item{at}{Named list fixing particular values, for instance
\code{list(age = c(40, 60))}.}

\item{weights}{\code{"equal"}, \code{"proportional"} or \code{"cells"}; see above.}

\item{type}{\code{"link"} or \code{"response"}.}

\item{level}{Confidence level.}
}
\value{
An object of class \code{"ilm_emm"}: a data frame of the grid with
\code{estimate}, \code{se}, \code{lower}, \code{upper}, plus the contrast machinery it carries.
}
\description{
The model's predicted mean for each level of the variables asked for, with
the other predictors averaged over rather than left at whatever value
happens to be first.
}
\section{What is averaged, and how}{


A marginal mean is a prediction on a grid of every factor combination, with
numeric predictors held at their means. The factors \emph{not} named in \code{specs}
have to be averaged over, and how they are averaged changes the answer
whenever the design is unbalanced:
\itemize{
\item \code{weights = "equal"} treats every cell alike, answering "what would these
groups look like in a balanced design". This is the usual default and
matches what most software calls an estimated marginal mean.
\item \code{weights = "proportional"} weights each cell by the product of the
\emph{marginal} frequencies of the variables being averaged over, answering
"what do these groups look like in a population with this sample's
composition".
\item \code{weights = "cells"} weights by the \emph{joint} frequency actually observed,
so each group is averaged over its own covariate distribution.
}

The first two keep a comparison clean: the averaged-over part is identical
for every level of \code{specs}, so a difference between two marginal means is
the model's effect and nothing else. \code{"cells"} does not, because each group
gets its own mix. When the averaged-over variables are associated with
\code{specs}, a difference of cell-weighted means carries that difference in
composition as well as the effect, and \code{\link[=ilm_contrast]{ilm_contrast()}} says so. That is
sometimes the quantity you want -- it describes the groups as they are --
but it is not an adjusted comparison.
}

\section{Which scale}{


Marginal means are computed on the \strong{link} scale, where they are exact
linear combinations of the coefficients and their variance is \verb{L V L'} with
no approximation. \code{type = "response"} back-transforms the endpoints, which
keeps the interval's coverage but means the reported centre is a median
rather than a mean on that scale. For a model with a random effect that
centre is also conditional on the group rather than population-averaged --
\code{\link[=ilm_ame]{ilm_ame()}} is the population-averaged quantity.
}

\examples{
set.seed(1); n <- 200
d <- data.frame(g = factor(sample(c("a", "b", "c"), n, TRUE)), x = rnorm(n))
d$y <- 1 + 0.5 * (d$g == "b") + 0.2 * d$x + rnorm(n)
fit <- ilm_model(y ~ g + x, data = d, family = "gaussian", verbose = FALSE)
ilm_emmeans(fit, "g")
}
\seealso{
\code{\link[=ilm_contrast]{ilm_contrast()}} to compare them, \code{\link[=ilm_ame]{ilm_ame()}} for the average
marginal effect of a predictor.
}
