% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_check.R, R/zzz-iml-aliases.R
\name{ilm_check_variance}
\alias{ilm_check_variance}
\alias{iml_check_variance}
\title{Is the residual spread constant?}
\usage{
ilm_check_variance(
  object,
  by = NULL,
  B = 100L,
  seed = 1L,
  ncores = 1L,
  plot = TRUE,
  verbose = TRUE,
  progress = NULL
)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{by}{Optional name of a column in the model frame, or a vector, whose
levels might have different variances.}

\item{B}{Simulated datasets. A simulated p-value cannot fall below
\code{1 / (B + 1)}, so B must exceed 100 for the strongest verdict to be
reachable.}

\item{seed}{Random seed.}

\item{ncores}{Worker processes for the refits.}

\item{plot}{Draw the scale-location panel.}

\item{verbose}{Print the verdict.}

\item{progress}{Show a progress bar. Defaults to \code{\link[=interactive]{interactive()}}, so a
bar appears when someone is watching and nothing is written in a
script or a knitted document. See \link{ilm_progress_arg}.}
}
\value{
Invisibly, a list with the observed statistics, their simulated
nulls, p-values, a \code{status} and a suggested remedy.
}
\description{
The assumption illume had no check for. Heteroscedasticity does not usually
bias the coefficients much, but it does bias their standard errors, which is
what an inference package is for.
}
\details{
Two patterns are tested, because they call for different fixes: spread that
trends with the fitted value, and spread that differs between the levels of
a grouping variable. Both are measured on randomised quantile residuals,
which are standard normal when the model and its variance are both correct,
and both are compared against a null simulated from the fitted model rather
than against an assumed distribution.
}
\examples{
set.seed(1)
d <- ilm_sim(n_id = 25)
f <- ilm_model(score ~ income + (1 | id), data = d, family = "gaussian",
               verbose = FALSE)
ilm_check_variance(f, B = 20, plot = FALSE)
}
\seealso{
\code{\link[=ilm_rqr_test]{ilm_rqr_test()}} for other residual statistics,
\code{\link[=ilm_check_dispersion]{ilm_check_dispersion()}} when the whole response is over-dispersed rather
than unevenly dispersed.
}
