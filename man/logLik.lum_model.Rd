% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lum_methods.R
\name{logLik.lum_model}
\alias{logLik.lum_model}
\alias{nobs.lum_model}
\title{Log-likelihood, and number of observations}
\usage{
\method{logLik}{lum_model}(object, ...)

\method{nobs}{lum_model}(object, ...)
}
\arguments{
\item{object}{A fitted \code{"lum_model"} object.}

\item{...}{Unused.}
}
\value{
An object of class \code{"logLik"}.

An object of class \code{"logLik"}.
}
\description{
Returns the \strong{Laplace-approximate} marginal log-likelihood -- the likelihood
with the random effects integrated out, using the approximation described in
\code{\link[=lum_fit]{lum_fit()}}. The \code{df} attribute counts fixed effects \emph{and} covariance
parameters, which is the convention \code{\link[=AIC.lum_model]{AIC.lum_model()}} and \code{\link[=BIC.lum_model]{BIC.lum_model()}} use.

Returns the \strong{Laplace-approximate} marginal log-likelihood -- the likelihood
with the random effects integrated out, using the approximation described in
\code{\link[=lum_fit]{lum_fit()}}. The \code{df} attribute counts fixed effects \emph{and} covariance
parameters, which is the convention \code{\link[=AIC.lum_model]{AIC.lum_model()}} and \code{\link[=BIC.lum_model]{BIC.lum_model()}} use.
}
\details{
Because it is an approximation, differences between models are most
trustworthy when the random structure is held fixed, so that the
approximation error largely cancels.

Because it is an approximation, differences between models are most
trustworthy when the random structure is held fixed, so that the
approximation error largely cancels.
}
\seealso{
\code{\link[=AIC.lum_model]{AIC.lum_model()}}, \code{\link[=BIC.lum_model]{BIC.lum_model()}}, \code{\link[=lum_anova]{lum_anova()}}.

\code{\link[=AIC.lum_model]{AIC.lum_model()}}, \code{\link[=BIC.lum_model]{BIC.lum_model()}}, \code{\link[=lum_anova]{lum_anova()}}.
}
