% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lum_methods.R
\name{coef.lum_model}
\alias{coef.lum_model}
\alias{vcov.lum_model}
\title{Coefficients and their covariance matrix}
\usage{
\method{coef}{lum_model}(object, full = FALSE, ...)

\method{vcov}{lum_model}(object, full = FALSE, ...)
}
\arguments{
\item{object}{A fitted \code{"lum_model"} object.}

\item{full}{Logical. Include covariance parameters as well as fixed effects.}

\item{...}{Unused.}
}
\value{
A named numeric vector (\code{coef()}) or a named matrix (\code{vcov()}).

A named numeric vector (\code{coef()}) or a named matrix (\code{vcov()}).
}
\description{
\code{coef()} returns the fixed effects as a named vector and \code{vcov()} the matching
covariance matrix. Their names line up exactly, which is what \code{car::Anova()},
\code{emmeans} and \code{marginaleffects} rely on.

\code{coef()} returns the fixed effects as a named vector and \code{vcov()} the matching
covariance matrix. Their names line up exactly, which is what \code{car::Anova()},
\code{emmeans} and \code{marginaleffects} rely on.
}
\details{
Names take the form \code{category:predictor}, for example \code{low:x1}. Because
categories are coded sum-to-zero, \code{low:x1} is the effect of \code{x1} on the \code{low}
category \emph{relative to the average across categories}, not relative to a
baseline category.

\code{full = TRUE} appends the covariance parameters (entries of the Cholesky
factors) to the fixed effects. This matters for population-averaged
predictions, which depend on the random-effect covariance as well as the
fixed effects: a delta-method calculation given only the fixed effects would
silently report no uncertainty at all from the covariance parameters.

Names take the form \code{category:predictor}, for example \code{low:x1}. Because
categories are coded sum-to-zero, \code{low:x1} is the effect of \code{x1} on the \code{low}
category \emph{relative to the average across categories}, not relative to a
baseline category.

\code{full = TRUE} appends the covariance parameters (entries of the Cholesky
factors) to the fixed effects. This matters for population-averaged
predictions, which depend on the random-effect covariance as well as the
fixed effects: a delta-method calculation given only the fixed effects would
silently report no uncertainty at all from the covariance parameters.
}
\seealso{
\code{\link[=lum_coef_table]{lum_coef_table()}} for a formatted table with tests.

\code{\link[=lum_coef_table]{lum_coef_table()}} for a formatted table with tests.
}
