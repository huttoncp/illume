% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_interpret.R, R/zzz-iml-aliases.R
\name{ilm_interpret}
\alias{ilm_interpret}
\alias{ilm_interpret.ilm_model}
\alias{ilm_interpret.ilm_dag_model}
\alias{ilm_interpret.ilm_did}
\alias{ilm_interpret.ilm_rdd}
\alias{ilm_interpret.ilm_power}
\alias{ilm_interpret.ilm_contrast}
\alias{iml_interpret}
\title{Interpret a fitted model in words}
\usage{
ilm_interpret(object, ...)

\method{ilm_interpret}{ilm_model}(object, causal = NULL, ame = TRUE, digits = 3, ...)

\method{ilm_interpret}{ilm_dag_model}(object, causal = NULL, ame = TRUE, digits = 3, ...)

\method{ilm_interpret}{ilm_did}(object, causal = NULL, ame = FALSE, digits = 3, ...)

\method{ilm_interpret}{ilm_rdd}(object, causal = NULL, ame = FALSE, digits = 3, ...)

\method{ilm_interpret}{ilm_power}(
  object,
  causal = NULL,
  ame = FALSE,
  digits = 2,
  target = 0.8,
  ...
)

\method{ilm_interpret}{ilm_contrast}(object, causal = NULL, ame = FALSE, digits = 3, ...)
}
\arguments{
\item{object}{An \code{\link[=ilm_model]{ilm_model()}}, \code{\link[=ilm_dag_model]{ilm_dag_model()}}, \code{\link[=ilm_did]{ilm_did()}} or
\code{\link[=ilm_rdd]{ilm_rdd()}}; or the result of \code{\link[=ilm_power]{ilm_power()}} or \code{\link[=ilm_power_design]{ilm_power_design()}}, which
is written up as a power analysis, or of \code{\link[=ilm_contrast]{ilm_contrast()}}, written up as
the comparisons it makes.}

\item{...}{Unused.}

\item{causal}{Force causal or associational language. \code{NULL} decides from
the design, which is what you want.}

\item{ame}{Report average marginal effects on the response scale. Costs a
delta-method calculation; worth it for anything with a link function.}

\item{digits}{Rounding.}

\item{target}{For a power analysis, the power a study is to reach.}
}
\value{
An object of class \code{"ilm_interpretation"}: a list of sections, which
\code{print()} renders as wrapped text.
}
\description{
Writes out what a fit says: each effect on the scale the response is measured
on, how strong the evidence is, what the diagnostics found, and what that
implies for taking the estimates at face value. Intended for reporting to
people who will not read a coefficient table.
}
\section{Causal language is licensed, not assumed}{


A regression coefficient is an association. This says "associated with"
unless the object carries a design that identifies an effect -- an
\code{\link[=ilm_dag_model]{ilm_dag_model()}} with a valid adjustment set, an \code{\link[=ilm_did]{ilm_did()}}, an
\code{\link[=ilm_rdd]{ilm_rdd()}} -- in which case it says so and names what licenses it. That is
the single most common error in reporting a model, and the one place an
automatic interpreter could do real damage, so it is handled explicitly.
}

\section{What it will not do}{


It says nothing about bias that a diagnostic did not measure. Every such
sentence traces to a check that was actually run, and names the remedy that
check names. Where no diagnostic has spoken, it is silent rather than
reassuring: a clean report from this function means the checks that ran
passed, not that the model is right.

The prose is templated. The same fit gives the same words every time.
}

\examples{
set.seed(1); n <- 300
d <- data.frame(x = rnorm(n), g = factor(sample(c("a", "b"), n, TRUE)))
d$y <- 0.5 * d$x + 0.8 * (d$g == "b") + rnorm(n)
fit <- ilm_model(y ~ x + g, data = d, family = "gaussian", verbose = FALSE)
ilm_interpret(fit, ame = FALSE)
}
\seealso{
\code{\link[=ilm_ame]{ilm_ame()}}, \code{\link[=ilm_appraise]{ilm_appraise()}}.
}
