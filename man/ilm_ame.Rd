% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_interpret.R, R/zzz-iml-aliases.R
\name{ilm_ame}
\alias{ilm_ame}
\alias{iml_ame}
\title{Average marginal effect on the response scale}
\usage{
ilm_ame(
  object,
  terms = NULL,
  eps = 1e-04,
  groups = c("typical", "population"),
  marginal = NULL
)
}
\arguments{
\item{object}{An \code{\link[=ilm_model]{ilm_model()}}.}

\item{terms}{Which predictors. Default is every fixed-effect term.}

\item{eps}{Relative step for the numerical derivatives.}

\item{groups}{\code{"typical"} (the default) for the effect in a typical group,
with the random effects at zero; \code{"population"} for the effect averaged
over them. See "Which effect, in a mixed model".}

\item{marginal}{Deprecated. \code{TRUE} is \code{groups = "population"}, and \code{FALSE}
is \code{groups = "typical"}.}
}
\value{
A data frame with \code{term}, \code{level}, \code{estimate}, \code{se}, \code{lower},
\code{upper}, and \code{kind} (\code{"slope"} or \code{"contrast"}). For an outcome with
categories -- multinomial or ordinal -- there is also a \code{category} column
and one row per category: the effect on the probability of each. Those
rows sum to zero for every term, because the probabilities sum to one.
}
\description{
What a coefficient means where the response lives. For a gaussian model that
is the coefficient itself; for anything with a link it is not, and the
difference matters: an odds ratio of 2 can be a 3-point change in probability
or a 20-point one depending on where the data sit.
}
\details{
For a numeric predictor this is the average of the derivative of the fitted
mean with respect to it, taken over the rows actually observed. For a factor
it is the average change in fitted mean from moving every row to that level
from the reference, which is a contrast rather than a derivative.

Standard errors come from the delta method: the effect is differentiated
numerically with respect to the parameters and combined with their covariance.
The parameter vector includes the covariance parameters, which the effect
depends on when it is averaged over the random effects.
}
\section{Which effect, in a mixed model}{

By default, \code{groups = "typical"}, the predictions hold the random effects at
zero, so the effect is the one for a \strong{typical group} -- the effect
\code{\link[=ilm_interpret]{ilm_interpret()}} reports, and the one the coefficients describe. With
\code{groups = "population"} the predictions are averaged over every random term,
as \code{predict(groups = "population")} does, and the effect is the one on the
\strong{population as a whole}. Through a nonlinear link the two differ: a
logit's population-averaged effect is the flatter one. They answer
different questions. The \code{marginaleffects} bridge averages over the random
effects unless its option \code{ilm_model.groups} is set to \code{"typical"}, so by
default it matches \code{groups = "population"}.

"Marginal" is the usual name for this kind of effect, a derivative or a
contrast averaged over the rows, whichever groups it is taken for. The old
argument \code{marginal} chose the groups: \code{marginal = FALSE} is \code{groups = "typical"}, and \code{marginal = TRUE} is \code{groups = "population"}. It still
works, with a warning, and will be removed after the next release.
}

\examples{
set.seed(1); n <- 400
d <- data.frame(x = rnorm(n), g = factor(sample(c("a", "b"), n, TRUE)))
d$y <- rbinom(n, 1, plogis(0.4 * d$x + 0.6 * (d$g == "b")))
fit <- ilm_model(y ~ x + g, data = d, family = "binomial", verbose = FALSE)
ilm_ame(fit)
}
\seealso{
\code{\link[=ilm_interpret]{ilm_interpret()}}, and the \code{marginaleffects} package, which does
this and a great deal more once \code{\link[=ilm_register_marginaleffects]{ilm_register_marginaleffects()}} is called.
}
