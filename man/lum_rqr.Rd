% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lum_diagnostics.R
\name{lum_rqr}
\alias{lum_rqr}
\title{Randomised quantile residuals for a nominal outcome}
\usage{
lum_rqr(object, conditional = TRUE, seed = 1L)
}
\arguments{
\item{object}{A fitted \code{"lum_model"} object.}

\item{conditional}{Logical. Evaluate at the fitted random effects.}

\item{seed}{Integer. Random seed; the transform uses randomisation.}
}
\value{
A numeric vector on the unit interval.
}
\description{
Turns categorical outcomes into residuals that should be uniformly
distributed when the model is right.
}
\section{The obstacle, and the way around it}{

Quantile residuals need a cumulative distribution function, and a CDF needs an
ordering. Nominal categories have none -- "red", "green", "blue" cannot be put
in order -- which is why \code{DHARMa} cannot be applied directly to this model.

The resolution is that you do not need an ordering of \emph{categories}, only of
some number, and the model supplies one. For each observation the \strong{log
score} \code{-log(p)} at the observed category is a real-valued quantity whose
distribution under the model is completely known: it takes the value
\code{-log(p_j)} with probability \code{p_j}. The standard randomised transform then
applies, and the fitted probabilities themselves provide the ordering.
}

\section{Do not test these against a uniform distribution}{

Verified by simulation: when the probabilities are known exactly the residuals
are uniform, as intended. But \strong{in sample} they are not, because the random
effects were fitted to the same data, which pulls the observed log score down.
A Kolmogorov-Smirnov test against uniform therefore rejects a correctly
specified model. Use \code{\link[=lum_rqr_test]{lum_rqr_test()}}, which builds its reference by
refitting simulated data and so accounts for this automatically.
}

\references{
Dunn, P. K., & Smyth, G. K. (1996). Randomized quantile residuals. \emph{Journal of
Computational and Graphical Statistics}, 5(3), 236--244.

Hartig, F. (2022). DHARMa: Residual diagnostics for hierarchical regression
models. R package. (The simulation-based approach this borrows from.)
}
\seealso{
\code{\link[=lum_rqr_test]{lum_rqr_test()}}, \code{\link[=lum_appraise]{lum_appraise()}}.
}
