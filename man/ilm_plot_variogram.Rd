% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_variogram.R, R/zzz-iml-aliases.R
\name{ilm_plot_variogram}
\alias{ilm_plot_variogram}
\alias{iml_plot_variogram}
\title{Plot a residual variogram}
\usage{
ilm_plot_variogram(
  res,
  colour = "grey25",
  fill = "grey85",
  alpha = NULL,
  size = 1,
  main = NULL
)
}
\arguments{
\item{res}{The value returned by \code{\link[=ilm_variogram]{ilm_variogram()}}.}

\item{colour}{Colour for bins that are not flagged.}

\item{fill}{Envelope fill.}

\item{alpha}{Envelope transparency.}

\item{size}{Point scaling.}

\item{main}{Title.}
}
\value{
Invisibly, the table behind the plot.
}
\description{
Plot a residual variogram
}
\examples{
set.seed(1)
d <- data.frame(id = factor(rep(1:20, each = 5)),
                t = as.vector(replicate(20, sort(sample(1:20, 5)))),
                x = rnorm(100))
d$y <- d$x + rnorm(100)
f <- ilm_model(y ~ x + (1 | id), data = d, family = "gaussian",
               verbose = FALSE)
v <- ilm_variogram(f, d$t, d$id, breaks = 4, B = 15, plot = FALSE,
                   verbose = FALSE)
ilm_plot_variogram(v)
}
\seealso{
\code{\link[=ilm_variogram]{ilm_variogram()}}.
}
