% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_accessors.R, R/zzz-iml-aliases.R
\name{ilm_varcorr}
\alias{ilm_varcorr}
\alias{VarCorr.ilm_model}
\alias{as.data.frame.ilm_VarCorr}
\alias{iml_varcorr}
\title{Variance components of a fitted model}
\usage{
ilm_varcorr(object)

\method{VarCorr}{ilm_model}(x, sigma = 1, ...)

\method{as.data.frame}{ilm_VarCorr}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{object, x}{A fitted \code{"ilm_model"}.}

\item{sigma}{Unused; for the generic.}

\item{...}{Unused.}

\item{row.names, optional}{Unused; for the generic.}
}
\value{
An object of class \code{"ilm_VarCorr"}: a list with \code{re} (one
covariance matrix per random term, with \code{"stddev"} and \code{"correlation"}
attributes as \code{lme4} gives them), \code{latent} (the correlation over time, or
\code{NULL}), \code{dispersion} (\code{value}, \code{meaning}, \code{modelled}, or \code{NULL}) and
\code{family}. \code{as.data.frame()} gives \code{lme4}'s columns: \code{grp}, \code{var1},
\code{var2}, \code{vcov}, \code{sdcor}.
}
\description{
Each random term's covariance, a correlation over time's parameters and
the family's dispersion, all on their natural scale: what \code{lme4::VarCorr()}
reports, for every structure illume fits.
}
\details{
\strong{Random terms.} For one linear predictor, a term's covariance is that of
its coefficients: an intercept's variance, or an intercept and slopes'
variances and covariances. For a multinomial outcome it is the covariance
across categories, with the within-group structure beside it as the
\code{"Sigma_d"} attribute, whose \verb{[1, 1]} the fit holds at one because the
category covariance carries the scale.

\strong{Over time.} An AR(1) term's \code{Sigma} is its stationary covariance and
\code{rho} the correlation one grid step apart; a CAR(1) term's \code{rho} is per unit
of time, and \code{range} its reciprocal decay; a random walk has
\code{var_per_time}, the variance of a step one time unit long. The words in
\code{meaning} are those of \code{\link[=ilm_cells]{ilm_cells()}}.

\strong{Dispersion.} On the natural scale and named for what it is. For a
gaussian model it is \code{sigma}, the residual standard DEVIATION, not the
variance. With a dispersion model it varies by row, and this is its value
at the median row.

These are the variance parameters transformed exactly as a draw of them
would be -- through the one transform, \code{ilm_rebuild()} -- so that anything
summing over draws and this function cannot disagree at the estimate.

With \code{nlme} or \code{lme4} attached, \code{VarCorr(fit)} gives the same.
}
\examples{
set.seed(1)
d <- data.frame(id = factor(rep(1:12, each = 6)), t = rep(0:5, 12))
d$y <- 1 + rnorm(12)[d$id] + rnorm(12, 0, 0.3)[d$id] * d$t + rnorm(72)
fit <- ilm_model(y ~ t + (1 + t | id), data = d, family = "gaussian",
                 verbose = FALSE)
ilm_varcorr(fit)
as.data.frame(ilm_varcorr(fit))
}
\seealso{
\code{\link[=ilm_ranef]{ilm_ranef()}}, \code{\link[=ilm_cells]{ilm_cells()}}.
}
