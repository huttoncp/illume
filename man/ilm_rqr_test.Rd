% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_diagnostics.R, R/zzz-iml-aliases.R
\name{ilm_rqr_test}
\alias{ilm_rqr_test}
\alias{iml_rqr_test}
\title{Test residuals against a simulated reference}
\usage{
ilm_rqr_test(
  object,
  B = 30L,
  ncores = 1L,
  seed = 1L,
  stat = function(u) mean(u),
  verbose = TRUE
)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{B}{Integer. Simulated datasets.}

\item{ncores}{Integer. Worker processes.}

\item{seed}{Integer. Random seed.}

\item{stat}{Function taking the residual vector and returning one number.}

\item{verbose}{Logical. Print the result.}
}
\value{
Invisibly, a list with \code{status}, the observed statistic, the simulated
null, and a z score and p-value.
}
\description{
Takes any single-number summary of the residuals and compares it to the
distribution that summary takes when the model is true, obtained by simulating
from the fit and refitting each replicate.
}
\details{
The refitting matters: it builds in the fact that in-sample residuals are not
exactly uniform (see \code{\link[=ilm_rqr]{ilm_rqr()}}), so a correctly specified model is not
flagged for a discrepancy that fitting itself created.
}
\section{What this can and cannot detect}{

Broad summaries of the residuals have essentially \strong{no power} against an
omitted covariate. This is structural rather than a matter of tuning: the
reference distribution is "this model refitting data it generated", and a
misspecified model reproduces its own behaviour faithfully, so the
misspecification cancels from both sides of the comparison.

A statistic aimed at specific structure does have power. Use
\code{\link[=ilm_check_covariate]{ilm_check_covariate()}} or \code{\link[=ilm_check_omitted]{ilm_check_omitted()}} to point the test at
particular variables, including ones the model does not contain.
}

