% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_effects.R, R/zzz-iml-aliases.R
\name{ilm_effects}
\alias{ilm_effects}
\alias{iml_effects}
\title{Effect sizes for the fixed effects}
\usage{
ilm_effects(object, level = 0.95, standardise = FALSE, vcov = NULL)
}
\arguments{
\item{object}{A fitted \code{\link[=ilm_model]{ilm_model()}}.}

\item{level}{Confidence level.}

\item{standardise}{\code{FALSE}, \code{TRUE} (one standard deviation) or \code{"gelman"}
(two), for numeric predictors only.}

\item{vcov}{Optional covariance matrix to use instead of the model's, for
instance from \code{\link[=ilm_vcov_cluster]{ilm_vcov_cluster()}} or \code{\link[=ilm_svy_vcov]{ilm_svy_vcov()}}.}
}
\value{
A data frame of \code{term}, \code{estimate} (on the link scale), \code{se},
\code{effect} (on the family's scale), \code{lower}, \code{upper}, \code{z} and \code{p}.
}
\description{
Reports each coefficient on the scale its family puts it on -- an odds
ratio, an incidence rate ratio, a time ratio, a hazard ratio -- with an
interval built on the link scale and transformed, which is exact and
asymmetric rather than symmetric and possibly negative.
}
\details{
For a gaussian response there is no ratio to take, so it reports the
coefficient, the standardised coefficient, and the partial variance each
term explains.
}
\section{Conditional against marginal}{


In a model with random effects, \code{exp(beta)} is the ratio for a \strong{given
cluster}: two patients in the same hospital, one exposed. It is not the
ratio for the population, and the gap widens as the random effects grow.
Which one a reader wants is almost always the population one, and which one
they are usually given is this one. It is labelled, and \code{\link[=ilm_ame]{ilm_ame()}} computes
the population-averaged quantity.
}

\section{Standardised coefficients}{


\code{standardise = TRUE} divides each numeric predictor's coefficient by that
predictor's standard deviation, so it reads per standard deviation. Binary
and categorical predictors are left alone: dividing by the standard
deviation of a 0/1 variable answers what happens per standard deviation of
treatment, which is not a question anyone asks. Gelman's suggestion of
dividing by \strong{two} standard deviations makes a numeric predictor
comparable with a binary one; \code{standardise = "gelman"} does that.
}

\examples{
set.seed(1); n <- 400
d <- data.frame(x = rnorm(n), g = factor(sample(c("a", "b"), n, TRUE)))
d$y <- rbinom(n, 1, plogis(-0.4 + 0.8 * d$x + 0.5 * (d$g == "b")))
f <- ilm_model(y ~ x + g, data = d, family = "binomial", verbose = FALSE)
ilm_effects(f)
}
\seealso{
\code{\link[=ilm_ame]{ilm_ame()}} for the population-averaged effect,
\code{\link[=ilm_emmeans]{ilm_emmeans()}} for group means, \code{\link[=ilm_vcov_cluster]{ilm_vcov_cluster()}} for a robust
covariance to pass in.
}
