% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_cor.R, R/zzz-iml-aliases.R
\name{ilm_rw1}
\alias{ilm_rw1}
\alias{iml_rw1}
\title{A random walk over time}
\usage{
ilm_rw1(time, group, verbose = TRUE)
}
\arguments{
\item{time}{Time, one value per observation: any numeric scale, a \code{Date} or
a date-time. The variance is per unit of it. Or a one-sided formula
\code{~ time | group} naming two columns of the model's data.}

\item{group}{Unit identifier, one value per observation. Omitted when
\code{time} is a formula.}

\item{verbose}{Warn about nearly coincident times.}
}
\value{
A \code{"ilm_rw1"} specification, to pass as \code{ilm_model(ar = )}.
}
\description{
Observations of the same unit share a latent level that wanders: each step
adds an independent change whose variance is proportional to the time that
passed, so the level has no mean to return to. This is the local-level
model of structural time series. Use it when a unit's level drifts rather
than fluctuating about a fixed value -- where \code{\link[=ilm_car1]{ilm_car1()}} would put its
correlation at the edge of its range.
}
\details{
\strong{The walk starts at zero.} Each group's walk is held at zero at its first
time, so its level there is the one the fixed effects give, and every later
value is measured from it. Without that anchor the walk and the intercept
would describe the same thing and neither could be estimated. It also means
the groups are taken to start from a common level, give or take the fixed
effects; where they do not, add a random intercept, \code{(1 | group)}, and each
starts where it does.

\strong{The variance is per unit of time.} Between two times \code{d} units apart the
walk moves by a normal change with variance \code{d} times the fitted variance,
which the fitted model holds as \code{Sigma$ar}. So the units of \code{time} matter:
days and weeks give variances seven times apart for the same walk. Irregular
gaps enter as they are, and cost nothing in accuracy.

\strong{What is estimated.} One latent value per distinct (group, time) pair
after each group's first. With a gaussian response the Laplace approximation
is exact, so the fit is the exact likelihood of the local-level model, by
maximum likelihood or by REML (\code{reml = TRUE}). With other families it is
the Laplace approximation, and the fit-time checks judge the number of
observations per latent value as they do for \code{\link[=ilm_car1]{ilm_car1()}}.

\code{ilm_rw1(~ time | group)} names two columns of the model's data instead of
passing them; see \code{\link[=ilm_ar1]{ilm_ar1()}} for why that is the safer form. It is also
what lets \code{predict(groups = "population")} place new rows on the walk,
whose variance depends on how long after its group's first time each one
falls.
}
\examples{
set.seed(1)
d <- data.frame(t = 1:60, unit = "a")
d$y <- 2 + cumsum(c(0, rnorm(59, 0, 0.7))) + rnorm(60)
fit <- ilm_model(y ~ 1, data = d, ar = ilm_rw1(~ t | unit),
                 verbose = FALSE)
fit$Sigma$ar                 # variance of the walk per unit of time
head(ilm_cells(fit))
}
\references{
Durbin, J., & Koopman, S. J. (2012). \emph{Time Series Analysis by State Space
Methods} (2nd ed.). Oxford University Press. (Chapter 2, the local level
model.)
}
\seealso{
\code{\link[=ilm_car1]{ilm_car1()}} for a process that returns to its mean,
\code{\link[=ilm_cells]{ilm_cells()}} for the fitted cells.
}
