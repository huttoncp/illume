% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_season.R
\name{ilm_fourier}
\alias{ilm_fourier}
\title{Fourier terms for a cycle of known period}
\usage{
ilm_fourier(x, period, K = 1L)
}
\arguments{
\item{x}{Time, as a number, integer or \code{Date}. One unit is one step, so the
period is expressed in the same units.}

\item{period}{Length of one full cycle: 12 for months of a year, 7 for days
of a week, 365.25 for days of a year.}

\item{K}{Number of harmonics.}
}
\value{
A numeric matrix with one row per element of \code{x} and \code{2 * K} columns
(one fewer when \code{2 * K} equals \code{period}, where the last sine column is
identically zero).
}
\description{
Sine and cosine columns for use directly in a model formula, when the
response has a repeating cycle -- a month-of-year effect in monthly data, a
day-of-week effect in daily data, a tide, a shift pattern.
}
\details{
\code{K} sets how much shape the cycle is allowed to have. \code{K = 1} is a single
smooth peak and trough per cycle; higher \code{K} adds harmonics and can bend into
sharper or double-peaked shapes, at two more columns each. A cycle of length
\code{period} supports at most \code{floor(period / 2)} harmonics, beyond which the
columns are not distinguishable at the observed times.

Unlike \code{\link[stats:poly]{stats::poly()}} this depends on nothing but its arguments, so a model
containing it predicts correctly on new data, including times outside the
range that was fitted.
}
\section{Choosing a basis}{

A factor for the phase -- \code{factor(month)} -- fits any shape at all, but
spends \code{period - 1} degrees of freedom doing it, and gives no reason for
neighbouring phases to resemble each other. Fourier terms spend \verb{2K} and are
smooth by construction. For a 12-month cycle, \code{K = 2} (four columns) covers
most real seasonal shapes.

Go to a factor when the cycle has a genuine step in it, such as a policy that
starts in April: measured on a narrow two-month spike, a factor beat both
smooth bases by more than 140 AIC at matched sample size, because no smooth
basis can make a step cheaply. See \code{\link[=ilm_cyclic]{ilm_cyclic()}} for the local alternative.
}

\examples{
head(ilm_fourier(1:24, period = 12))

set.seed(1)
d <- data.frame(id = factor(rep(1:20, each = 24)), t = rep(1:24, 20))
d$y <- 2 * sin(2 * pi * d$t / 12) + rnorm(480)
f <- ilm_model(y ~ ilm_fourier(t, 12) + (1 | id), data = d,
               family = "gaussian", verbose = FALSE)
coef(f)
}
\seealso{
\code{\link[=ilm_check_ar]{ilm_check_ar()}}, which names the period when the residuals contain
a cycle.
}
