% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_dag.R
\name{ilm_dag}
\alias{ilm_dag}
\title{A causal graph}
\usage{
ilm_dag(x, exposure = NULL, outcome = NULL, latent = NULL)
}
\arguments{
\item{x}{A dagitty-style string, a data frame of edges, or a \code{dagitty}
object.}

\item{exposure, outcome}{Variable names. Optional if the string or object
already declares them; required otherwise before an adjustment set can be
found.}

\item{latent}{Names of unobserved variables, added to any the input declares.}
}
\value{
An object of class \code{"ilm_dag"}.
}
\description{
Holds a directed acyclic graph, which variables are unobserved, and which
are the exposure and the outcome. This is the object \code{\link[=ilm_adjust_sets]{ilm_adjust_sets()}} and
\code{\link[=ilm_dag_test]{ilm_dag_test()}} work from.
}
\details{
Three input forms, all giving the same object:
\itemize{
\item a dagitty-style string, with or without the \verb{dag \{ \}} wrapper;
\item a data frame of edges with columns \code{from} and \code{to};
\item a \code{dagitty} object, if that package is installed.
}

A bidirected edge \verb{X <-> Y} states that something unobserved causes both, so
it is stored as an unobserved node with an arrow into each. That keeps one
representation -- a DAG, possibly with unobserved nodes -- rather than two.
}
\examples{
g <- ilm_dag("dag {
  smoking [exposure]
  cancer  [outcome]
  smoking -> tar -> cancer
  genotype -> smoking
  genotype -> cancer
}")
g
}
\seealso{
\code{\link[=ilm_adjust_sets]{ilm_adjust_sets()}} for what the graph licenses, \code{\link[=ilm_dag_test]{ilm_dag_test()}}
for whether the data agree with it.
}
