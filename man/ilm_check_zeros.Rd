% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_check.R, R/zzz-iml-aliases.R
\name{ilm_check_zeros}
\alias{ilm_check_zeros}
\alias{iml_check_zeros}
\title{Are there more zeros than the model expects?}
\usage{
ilm_check_zeros(object, B = 200L, seed = 1L)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{B}{Simulated datasets. A simulated p-value cannot fall below
\code{1 / (B + 1)}, so B must exceed 100 for the strongest verdict to be
reachable at all.}

\item{seed}{Random seed.}
}
\value{
Invisibly, a list with the observed and expected zero counts, a
p-value and a \code{status}.
}
\description{
More zeros than a count model expects is the one misspecification that
cannot be read off a residual plot, because a zero is a perfectly ordinary
value of a count. This simulates from the fit and asks how often it produces
as many zeros as were seen.
}
\details{
The remedy is \link[=ilm_model]{ilm_model(ziformula = )}, which adds a second
linear predictor for the probability of an excess zero. Whether that should
be a mixture (\code{zi_type = "inflated"}) or two processes (\code{"hurdle"}) is a
question about what the zeros mean rather than one this check can answer.
}
\examples{
set.seed(1)
d <- ilm_sim()
f <- ilm_model(downtime ~ income + (1 | id), data = d, family = "poisson",
               verbose = FALSE)
ilm_check_zeros(f, B = 200)
}
