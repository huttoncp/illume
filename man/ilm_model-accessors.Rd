% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_model.R
\name{terms.ilm_model}
\alias{terms.ilm_model}
\alias{formula.ilm_model}
\alias{model.frame.ilm_model}
\alias{model.matrix.ilm_model}
\alias{getCall.ilm_model}
\title{Standard accessors for a fitted model}
\usage{
\method{terms}{ilm_model}(x, ...)

\method{formula}{ilm_model}(x, ...)

\method{model.frame}{ilm_model}(formula, ...)

\method{model.matrix}{ilm_model}(object, ...)

\method{getCall}{ilm_model}(x, ...)
}
\arguments{
\item{x, object, formula}{A fitted \code{"ilm_model"} object.}

\item{...}{Unused.}
}
\value{
The model terms, formula, model frame, model matrix or original call.

The model terms, formula, model frame, model matrix or original call.

The model terms, formula, model frame, model matrix or original call.

The model terms, formula, model frame, model matrix or original call.

The model terms, formula, model frame, model matrix or original call.
}
\description{
Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.

Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.

Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.

Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.

Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.
}
