% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_diagnostics.R
\name{ilm_check_covariate}
\alias{ilm_check_covariate}
\title{Test whether residuals shift across a variable}
\usage{
ilm_check_covariate(
  object,
  x,
  name = NULL,
  nbin = 5L,
  B = 30L,
  ncores = 1L,
  seed = 1L,
  verbose = TRUE
)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{x}{A variable with one value per row of the data the model was fitted
to. Rows the fit dropped for missing values are removed automatically, so
the column can be passed straight from the original data frame. Numeric
variables are binned; factors are used as-is.}

\item{name}{Character label for printing.}

\item{nbin}{Integer. Bins for a numeric variable.}

\item{B}{Integer. Simulated datasets.}

\item{ncores}{Integer. Worker processes.}

\item{seed}{Integer. Random seed.}

\item{verbose}{Logical. Print the result.}
}
\value{
Invisibly, a list with \code{status}, the observed statistic and the test.
}
\description{
Asks whether residuals are systematically higher or lower at some values of a
variable than others. If they are, the model is missing something about that
variable -- it may need to be added, or added in a more flexible form.
}
\details{
The variable does \strong{not} have to be in the model; pointing the test at
variables you left out is exactly the useful case, and is the one situation
where residual testing has real power here. Broad summaries of residuals
detect almost nothing; a test aimed at specific structure detects a great
deal. See \code{\link[=ilm_rqr_test]{ilm_rqr_test()}} for why.
}
\seealso{
\code{\link[=ilm_check_omitted]{ilm_check_omitted()}}.
}
