% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_survey.R
\name{ilm_svy_coef}
\alias{ilm_svy_coef}
\title{Fixed effects with design-based standard errors}
\usage{
ilm_svy_coef(
  object,
  design = NULL,
  level = 0.95,
  lonely = c("adjust", "certainty", "fail")
)
}
\arguments{
\item{object}{A fitted \code{\link[=ilm_model]{ilm_model()}} from a complex sample.}

\item{design}{An \code{\link[=ilm_design]{ilm_design()}}, or \code{NULL} to use the fit's own.}

\item{level}{Confidence level.}

\item{lonely}{See \code{\link[=ilm_svy_vcov]{ilm_svy_vcov()}}.}
}
\value{
A data frame of \code{term}, \code{estimate}, \code{se}, \code{se_model}, \code{t}, \code{p},
\code{lower} and \code{upper}, on the design degrees of freedom.
}
\description{
Fixed effects with design-based standard errors
}
\examples{
set.seed(1); n <- 600
d <- data.frame(x = rnorm(n), psu = rep(1:60, each = 10),
                st = rep(1:2, each = 300))
d$w <- ifelse(d$st == 1, 40, 10)
d$y <- 1 + 0.5 * d$x + rep(rnorm(60, 0, 0.8), each = 10) + rnorm(n)
des <- ilm_design(d, weights = ~ w, ids = ~ psu, strata = ~ st)
fit <- ilm_model(y ~ x, data = d, family = "gaussian", design = des,
                 verbose = FALSE)
ilm_svy_coef(fit)
}
\seealso{
\code{\link[=ilm_design]{ilm_design()}}, \code{\link[=ilm_svy_vcov]{ilm_svy_vcov()}}.
}
