% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_robust.R
\name{ilm_robust}
\alias{ilm_robust}
\title{Fixed effects with sandwich standard errors}
\usage{
ilm_robust(
  object,
  cluster = NULL,
  type = c("CR2", "CR1", "CR0"),
  df = c("bm", "G-1", "normal"),
  level = 0.95
)
}
\arguments{
\item{object}{A fitted \code{\link[=ilm_model]{ilm_model()}} with no random effects.}

\item{cluster}{Clustering, as in \code{\link[=ilm_vcov_cluster]{ilm_vcov_cluster()}}. \code{NULL} gives
heteroskedasticity-robust standard errors.}

\item{type}{\code{"CR2"}, \code{"CR1"} or \code{"CR0"}.}

\item{df}{\code{"bm"} for Bell-McCaffrey (the default), \code{"G-1"}, \code{"normal"}, or a
single number to use for every coefficient.}

\item{level}{Confidence level.}
}
\value{
A data frame of \code{term}, \code{estimate}, \code{se}, \code{df}, \code{t}, \code{p}, \code{lower},
\code{upper}, with the model-based standard error alongside for comparison.
}
\description{
The same coefficients, re-tested against a covariance that does not assume
the variance structure is right. See \code{\link[=ilm_vcov_cluster]{ilm_vcov_cluster()}} for what the
corrections do.
}
\section{The degrees of freedom are the point}{


A sandwich is consistent in the number of clusters, so with few of them the
normal reference is badly optimistic. The default here is a t distribution
on the Bell-McCaffrey degrees of freedom, computed per coefficient from the
design rather than fixed at \code{G - 1}. Those can be far smaller than \code{G - 1}
when one cluster dominates a predictor -- a treatment assigned to three
schools out of thirty does not have twenty-nine degrees of freedom behind
it, and the number this reports is the warning.
}

\examples{
set.seed(1); G <- 30; n <- 12
d <- data.frame(g = factor(rep(seq_len(G), each = n)), x = rnorm(G * n))
d$y <- 1 + 0.4 * d$x + rep(rnorm(G, 0, 1.5), each = n) + rnorm(G * n)
f <- ilm_model(y ~ x, data = d, family = "gaussian", verbose = FALSE)
ilm_robust(f, ~ g)
}
\seealso{
\code{\link[=ilm_vcov_cluster]{ilm_vcov_cluster()}}.
}
