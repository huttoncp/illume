% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_trends.R
\name{ilm_trends}
\alias{ilm_trends}
\title{Estimated marginal slopes}
\usage{
ilm_trends(
  object,
  specs,
  var,
  at = NULL,
  weights = c("equal", "proportional", "cells"),
  delta = NULL,
  level = 0.95,
  df = "auto"
)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{specs}{Factor(s) to compute the slope within, as a character vector.}

\item{var}{The continuous predictor whose slope is wanted.}

\item{at}{Named list fixing other predictors, as in \code{\link[=ilm_emmeans]{ilm_emmeans()}}. The
slope of \code{var} is constant across these unless \code{var} enters non-linearly
or interacts with them, in which case where you evaluate it matters and
the result says so.}

\item{weights}{How to average over factors not in \code{specs}; see
\code{\link[=ilm_emmeans]{ilm_emmeans()}}.}

\item{delta}{Step for the numerical derivative. The default is 1/1000 of
the observed range of \code{var}, which is exact for any linear term and small
enough elsewhere.}

\item{level}{Confidence level.}

\item{df}{Denominator degrees of freedom for the within-level tests: one of
\code{"auto"}, \code{"satterthwaite"}, \code{"kenward-roger"}, \code{"residual"},
\code{"asymptotic"}, or a single number. See \code{\link[=ilm_denom_df]{ilm_denom_df()}}. \code{"auto"} gives
an exact t where nothing was integrated out, Satterthwaite for a gaussian
mixed model, and a z test otherwise.}
}
\value{
A data frame of class \code{"ilm_emm"}, one row per level of \code{specs},
with \code{estimate}, \code{se}, \code{df}, \code{statistic}, \code{p.value}, \code{lower} and \code{upper}.
Being an \code{"ilm_emm"} it can be passed straight to \code{\link[=ilm_contrast]{ilm_contrast()}}.
}
\description{
The slope of a continuous predictor within each level of a factor, with the
other predictors averaged over. This is what takes a significant
interaction between a factor and a covariate apart.
}
\section{The two questions, which are not the same question}{

A treatment-by-time interaction says the slope on time depends on the arm.
Two different follow-ups are then available, and they answer different
things:
\itemize{
\item \strong{Do the slopes differ between arms?} Pass the result to
\code{\link[=ilm_contrast]{ilm_contrast()}}, which differences the rows. This is the interaction
restated one pair at a time.
\item \strong{Is the slope different from zero within an arm?} Read the
\code{statistic} and \code{p.value} columns of this table, which test each row
against zero.
}

They can disagree in both directions: two arms can have slopes that differ
significantly while neither is distinguishable from zero, and both can be
strongly non-zero while not differing from one another. Reporting one and
describing it as the other is the common way a within-arm result gets
written up as a between-arm claim.
}

\section{Why a marginal mean cannot do this}{

\code{\link[=ilm_emmeans]{ilm_emmeans()}} averages predictions with the covariate held at its mean,
which collapses the very thing the interaction is about. The slope has to be
estimated as a slope. The two share all their machinery -- the same
reference grid, the same weighting, the same covariance -- and differ only
in what fills the contrast matrix.
}

\examples{
set.seed(1)
d <- data.frame(arm = factor(rep(c("ctl", "trt"), each = 60)),
                week = rep(0:5, 20))
d$y <- 2 + 0.1 * d$week + 0.4 * d$week * (d$arm == "trt") + rnorm(120)
f <- ilm_model(y ~ arm * week, data = d, family = "gaussian",
               verbose = FALSE)
tr <- ilm_trends(f, "arm", var = "week")
tr                      # is each arm's slope different from zero?
ilm_contrast(tr)        # do the two arms' slopes differ from each other?
}
\seealso{
\code{\link[=ilm_emmeans]{ilm_emmeans()}} for means, \code{\link[=ilm_contrast]{ilm_contrast()}} for differences
between the slopes, \code{\link[=ilm_anova]{ilm_anova()}} for the omnibus interaction this takes
apart.
}
