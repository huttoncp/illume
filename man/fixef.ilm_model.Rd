% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_methods.R, R/zzz-iml-aliases.R
\name{fixef.ilm_model}
\alias{fixef.ilm_model}
\alias{ilm_se_fixef}
\alias{iml_se_fixef}
\title{Fixed effects as a matrix}
\usage{
\method{fixef}{ilm_model}(object, ...)

ilm_se_fixef(object)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{...}{Unused.}
}
\value{
A numeric matrix with one row per design column and one column per
category dimension.

A numeric matrix with one row per design column and one column per
category dimension.
}
\description{
The same numbers as \code{\link[=coef.ilm_model]{coef.ilm_model()}} but shaped as predictors by categories,
which is usually easier to read for a multinomial model. \code{ilm_se_fixef()} returns
matching standard errors.

The same numbers as \code{\link[=coef.ilm_model]{coef.ilm_model()}} but shaped as predictors by categories,
which is usually easier to read for a multinomial model. \code{ilm_se_fixef()} returns
matching standard errors.
}
