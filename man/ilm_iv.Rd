% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_iv.R
\name{ilm_iv}
\alias{ilm_iv}
\title{Instrumental-variables regression by two-stage least squares}
\usage{
ilm_iv(
  formula,
  data,
  weights = NULL,
  cluster = NULL,
  robust = FALSE,
  na.action = stats::na.omit
)
}
\arguments{
\item{formula}{\code{y ~ x + w | z + w}; see above.}

\item{data}{A data frame.}

\item{weights}{Optional frequency weights.}

\item{cluster}{Optional one-sided formula or vector for cluster-robust
standard errors; see \code{\link[=ilm_vcov_cluster]{ilm_vcov_cluster()}}.}

\item{robust}{Use heteroskedasticity-robust standard errors. Implied by
\code{cluster}.}

\item{na.action}{How to handle missing values.}
}
\value{
An object of class \code{"ilm_iv"}: the coefficients, their standard
errors and tests, the diagnostics above, and the pieces they were built
from.
}
\description{
For a linear model in which one or more regressors are correlated with the
error -- a treatment people chose rather than were assigned, a price set in
response to demand, an exposure measured with error. An instrument moves the
offending regressor without acting on the outcome any other way, and the
part of the regressor it explains is used in its place.
}
\section{Writing the formula}{


\code{y ~ x + w | z + w}. To the left of the bar is the model you want. To the
right is the \strong{full} list of exogenous variables: the instruments, plus
every covariate that appears on both sides. Leaving \code{w} out of the right
side does not mean "w is fine", it means "w is endogenous too and has no
instrument", which is a different and usually unintended model. That case is
an error here rather than a silent one.
}

\section{What it reports without being asked}{

\itemize{
\item \strong{First-stage F}, one per endogenous regressor, on the excluded
instruments. Above 10 is the familiar rule of thumb; above about 104 is
what a conventional 5\% t-test actually needs, after Lee et al. (2022).
Both are shown, because the gap between them is the point.
\item \strong{Durbin-Wu-Hausman}, testing whether the regressor was endogenous at
all. If it was not, least squares is unbiased and far more precise, and
instrumenting has thrown that away for nothing.
\item \strong{Sargan's J}, when there are more instruments than endogenous
regressors, testing whether they agree with each other. It cannot test
whether they are all invalid together, which is the assumption that
matters and the one no test reaches.
}

When the instrument is weak the remedy is \code{\link[=ilm_iv_ar]{ilm_iv_ar()}}, which builds a
confidence set that stays valid however weak it is.
}

\examples{
set.seed(1); n <- 500
z <- rnorm(n); u <- rnorm(n)
x <- 0.8 * z + u + rnorm(n)          # x is correlated with the error u
y <- 1 + 0.5 * x + u + rnorm(n)
d <- data.frame(y = y, x = x, z = z)
coef(lm(y ~ x, data = d))            # biased upwards by the shared u
ilm_iv(y ~ x | z, data = d)
}
\references{
Lee, D. S., McCrary, J., Moreira, M. J. and Porter, J. (2022).
Valid t-ratio inference for IV. \emph{American Economic Review} 112, 3260-3290.
}
\seealso{
\code{\link[=ilm_iv_ar]{ilm_iv_ar()}} for weak-instrument-robust intervals,
\code{\link[=ilm_dag_model]{ilm_dag_model()}} for choosing an adjustment set when no instrument is
available.
}
