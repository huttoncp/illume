% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_aov_ez.R, R/zzz-iml-aliases.R
\name{ilm_aov_ez}
\alias{ilm_aov_ez}
\alias{iml_aov_ez}
\title{Factorial and repeated-measures ANOVA}
\usage{
ilm_aov_ez(
  id,
  dv,
  data,
  between = NULL,
  within = NULL,
  covariate = NULL,
  observed = NULL,
  engine = c("aov", "mixed"),
  reml = TRUE,
  fun_aggregate = NULL,
  posthoc = "auto",
  alpha = 0.05,
  correction = c("auto", "GG", "HF", "none"),
  verbose = TRUE
)
}
\arguments{
\item{id}{Column identifying the participant.}

\item{dv}{Column holding the numeric outcome.}

\item{data}{A long data frame: one row per participant per occasion.}

\item{between}{Factors that vary BETWEEN participants, as a character
vector.}

\item{within}{Factors that vary WITHIN participants.}

\item{covariate}{Numeric covariates. Must be constant within participant.}

\item{observed}{Variables measured rather than manipulated, affecting the
\code{ges} denominator. Covariates are included automatically.}

\item{engine}{\code{"aov"} (the default) computes the omnibus table classically
and fits a mixed model alongside. \code{"mixed"} makes the mixed model the
primary analysis, translating this specification into the corresponding
\code{\link[=ilm_model]{ilm_model()}} formula.}

\item{reml}{Passed to \code{\link[=ilm_model]{ilm_model()}} for the companion fit. \code{TRUE} here,
because the design fixed the fixed effects before any data were seen.}

\item{fun_aggregate}{Applied when a participant has several rows per cell.
Defaults to the mean, with a message saying it happened.}

\item{posthoc}{Follow a significant effect with pairwise comparisons.
\code{"auto"} does it for significant effects involving a factor with three or
more levels; \code{FALSE} never does; a character vector names effects.}

\item{alpha}{Threshold for deciding what \code{posthoc} follows up, and for
flagging sphericity.}

\item{correction}{Sphericity correction applied to the reported p-value:
\code{"auto"} (Greenhouse-Geisser when Mauchly's test rejects), \code{"GG"},
\code{"HF"}, or \code{"none"}.}

\item{verbose}{Narrate the checks.}
}
\value{
An object of class \code{"ilm_aov_ez"}: \code{anova} (the table),
\code{sphericity}, \code{fit} (the companion \code{\link[=ilm_model]{ilm_model()}}), \code{posthoc}, \code{notes},
and the design.
}
\description{
Specify the design by naming columns -- the participant, the outcome, which
factors vary between participants and which vary within them -- rather than
by writing a formula with an error term in it. Reports F on a denominator
degrees of freedom, mean squared error, generalized eta squared, and a
sphericity correction where one is needed.
}
\section{What it reports, and why not just a mixed model}{

On a balanced complete design a mixed model and a classical
repeated-measures ANOVA answer the same question. They report it
differently, and one of them reports things the other cannot:
\code{\link[=ilm_model]{ilm_model()}} gives a Wald chi-square, because once random effects are
integrated out there is no exact residual degrees of freedom to divide by.
An F, a mean squared error, a generalized eta squared and a
Greenhouse-Geisser correction do not fall out of that fit. So the omnibus
table here is computed classically, and a mixed model is fitted alongside
for the things the classical route cannot do -- marginal means, contrasts,
simple slopes, diagnostics, and an analysis that keeps participants with
incomplete data rather than dropping them.
}

\section{When the classical answer is the wrong one}{

Three situations make it the wrong tool, and each is checked and reported
with the alternative named rather than left to be noticed:
\itemize{
\item \strong{Sphericity fails.} The within-participant variances and covariances
are not what the F test assumes. Greenhouse-Geisser and Huynh-Feldt
corrections are reported, and so is the other option: an unstructured
mixed model does not assume sphericity, so it needs no correction at all.
\item \strong{Participants have missing cells.} Classical repeated measures has to
drop them completely, because a participant missing one occasion
contributes to no within-participant contrast. How many were dropped is
reported. A mixed model uses them.
\item \strong{A covariate varies within participant.} Refused, because a single
coefficient for a time-varying covariate blends two effects that can
point in opposite directions -- how participants who score higher on
average differ, and what happens when a participant scores higher than
usual.
}
}

\section{Generalized eta squared}{

\code{ges} is comparable across designs in a way that partial eta squared is
not, which is why it is the default here and in \code{afex}. Its denominator
includes the sums of squares of any \strong{measured} rather than manipulated
variable. Covariates are measured by definition, so they are added to
\code{observed} automatically; name any measured factors there too.
}

\examples{
set.seed(1)
n <- 24
d <- expand.grid(id = factor(seq_len(n)), time = factor(1:3))
d$grp <- factor(rep(c("ctl", "trt"), each = n / 2))[as.integer(d$id)]
d$score <- 10 + 2 * (d$grp == "trt") * as.integer(d$time) +
  rnorm(nrow(d)) + rep(rnorm(n), 3)
a <- ilm_aov_ez("id", "score", d, between = "grp", within = "time",
                verbose = FALSE)
a
}
\seealso{
\code{\link[=ilm_model]{ilm_model()}} for the general engine, \code{\link[=ilm_emmeans]{ilm_emmeans()}} and
\code{\link[=ilm_contrast]{ilm_contrast()}} for follow-ups, \code{\link[=ilm_trends]{ilm_trends()}} when the interaction is
with a continuous predictor.
}
