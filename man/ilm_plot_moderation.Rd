% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_plot_moderation.R
\name{ilm_plot_moderation}
\alias{ilm_plot_moderation}
\title{See a moderation}
\usage{
ilm_plot_moderation(
  x,
  moderator = NULL,
  exposure = NULL,
  at = NULL,
  level = 0.95,
  data = NULL,
  main = NULL,
  colour = NULL,
  pch = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{\link[=ilm_moderation]{ilm_moderation()}} result, or a fitted \code{\link[=ilm_model]{ilm_model()}} that
already contains the interaction.}

\item{moderator}{Which moderator to draw. Defaults to the strongest row.}

\item{exposure}{The treatment or exposure, when \code{x} is a plain model.}

\item{at}{For a continuous moderator, the values to evaluate at. Defaults to
its quartiles, so the picture spans the data rather than a nominal range.}

\item{level}{Confidence level.}

\item{data}{Needed only if a refit is required and the moderator is not in
the model frame.}

\item{main, colour, pch}{Passed through; \code{pch} takes a name.}

\item{...}{Passed to \code{\link[tinyplot:tinyplot]{tinyplot::tinyplot()}}.}
}
\value{
Invisibly, the data frame that was plotted.
}
\description{
Draws the effect of \code{x} at each level, or several values, of a moderator --
the picture behind an \code{\link[=ilm_moderation]{ilm_moderation()}} row.
}
\details{
For a categorical \code{x} this is the cell means, the interaction plot. For a
continuous \code{x} it is the \strong{slope} of \code{x} within each level of the
moderator, from \code{\link[=ilm_trends]{ilm_trends()}}, because a slope is the quantity being said
to differ. Both carry intervals.

This is deliberately not \code{\link[=ilm_plot_model]{ilm_plot_model()}}'s effect plot, which holds the
other predictors at typical values -- pinning the moderator is exactly what
would hide the moderation.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(tx = rbinom(300, 1, 0.5), age = rnorm(300),
                site = factor(sample(c("a", "b", "c"), 300, TRUE)))
d$y <- 0.4 * d$tx + 0.8 * d$tx * (d$site == "c") + rnorm(300)
fit <- ilm_model(y ~ tx + age + site, data = d, verbose = FALSE)
m <- ilm_moderation(fit, x = "tx", progress = FALSE)
ilm_plot_moderation(m)
}
}
\seealso{
\code{\link[=ilm_moderation]{ilm_moderation()}}, \code{\link[=ilm_trends]{ilm_trends()}}, \code{\link[=ilm_emmeans]{ilm_emmeans()}}.
}
