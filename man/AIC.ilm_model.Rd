% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_ic.R
\name{AIC.ilm_model}
\alias{AIC.ilm_model}
\alias{BIC.ilm_model}
\title{Information criteria}
\usage{
\method{AIC}{ilm_model}(object, ..., k = 2)

\method{BIC}{ilm_model}(object, ..., n = c("obs", "groups"))
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{...}{Unused.}

\item{k}{Numeric. Penalty per parameter; 2 gives AIC.}

\item{n}{\code{"obs"} or \code{"groups"}; \code{BIC()} only.}
}
\value{
A numeric scalar.

A numeric scalar.
}
\description{
AIC and BIC computed from the Laplace-approximate marginal log-likelihood,
with degrees of freedom counting both fixed effects and covariance
parameters.

AIC and BIC computed from the Laplace-approximate marginal log-likelihood,
with degrees of freedom counting both fixed effects and covariance
parameters.
}
\section{Three caveats worth knowing}{

\strong{The likelihood is approximate.} Differences between models are most
trustworthy when the random structure is the same in both, so that the
approximation error largely cancels.

\strong{Marginal AIC favours simpler random structures.} It is well suited to
comparing fixed-effect specifications; comparisons of \emph{random} structures are
biased toward the simpler model. Conditional AIC, which uses effective
degrees of freedom, is the right tool there and is not implemented.

\strong{BIC's sample size is ambiguous in a mixed model.} BIC penalises by
\code{log(n)}, but what counts as \code{n}? The number of observations, or the number of
independent groups? For covariance parameters the effective sample size is
closer to the group count. The default \code{n = "obs"} matches lme4 and glmmTMB so
values are comparable with those packages; \code{n = "groups"} gives the
alternative. They can select different models, and that is a genuine
disagreement rather than a bug.

A variance sitting at zero also makes the degrees of freedom an overcount,
since that parameter is not freely estimated, making both criteria
conservative.


\strong{The likelihood is approximate.} Differences between models are most
trustworthy when the random structure is the same in both, so that the
approximation error largely cancels.

\strong{Marginal AIC favours simpler random structures.} It is well suited to
comparing fixed-effect specifications; comparisons of \emph{random} structures are
biased toward the simpler model. Conditional AIC, which uses effective
degrees of freedom, is the right tool there and is not implemented.

\strong{BIC's sample size is ambiguous in a mixed model.} BIC penalises by
\code{log(n)}, but what counts as \code{n}? The number of observations, or the number of
independent groups? For covariance parameters the effective sample size is
closer to the group count. The default \code{n = "obs"} matches lme4 and glmmTMB so
values are comparable with those packages; \code{n = "groups"} gives the
alternative. They can select different models, and that is a genuine
disagreement rather than a bug.

A variance sitting at zero also makes the degrees of freedom an overcount,
since that parameter is not freely estimated, making both criteria
conservative.
}

\references{
Vaida, F., & Blanchard, S. (2005). Conditional Akaike information for
mixed-effects models. \emph{Biometrika}, 92(2), 351--370.

Delattre, M., Lavielle, M., & Poursat, M.-A. (2014). A note on BIC in
mixed-effects models. \emph{Electronic Journal of Statistics}, 8, 456--475.

Vaida, F., & Blanchard, S. (2005). Conditional Akaike information for
mixed-effects models. \emph{Biometrika}, 92(2), 351--370.

Delattre, M., Lavielle, M., & Poursat, M.-A. (2014). A note on BIC in
mixed-effects models. \emph{Electronic Journal of Statistics}, 8, 456--475.
}
