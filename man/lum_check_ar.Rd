% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lum_check_ar.R
\name{lum_check_ar}
\alias{lum_check_ar}
\title{Check for leftover temporal autocorrelation}
\usage{
lum_check_ar(
  object,
  time,
  group,
  maxlag = 8L,
  B = 30L,
  ncores = 1L,
  seed = 1L,
  verbose = TRUE,
  plot = FALSE
)
}
\arguments{
\item{object}{A fitted \code{"lum_model"} object.}

\item{time}{Integer time index, one per observation. Must be evenly spaced.}

\item{group}{Grouping variable, one per observation.}

\item{maxlag}{Integer. Largest lag to examine.}

\item{B}{Integer. Simulated datasets. No p-value can fall below \code{1/(B+1)}, so
\code{B} of about 100 is needed before the strongest verdict becomes reachable.}

\item{ncores}{Integer. Worker processes.}

\item{seed}{Integer. Random seed.}

\item{verbose}{Logical. Print the table.}

\item{plot}{Logical. Draw the autocorrelation and its band.}
}
\value{
Invisibly, a list with the per-lag table, per-category z scores and
the simulated null.
}
\description{
Tests whether residuals from observations close together in time within the
same group are more similar than the model predicts. If they are, the model
is missing temporal structure.
}
\section{Which residual, and why it matters}{

This uses signed one-against-the-rest Pearson residuals rather than the
log-score quantile residuals used elsewhere. The reason is worth
understanding: the log score depends only on the probability assigned to the
category that actually occurred, so it registers the \emph{size} of a discrepancy
but not its \strong{direction}. Autocorrelation is directional -- consecutive
observations being pushed the same way -- so a direction-blind residual cannot
see it. Measured side by side, the log-score residual had essentially no
power here while the signed Pearson residual separated the cases cleanly.
}

\section{What it cannot tell you}{

It detects autocorrelation the model does not \strong{account for}, so it fires on
a model fitted without an AR term to data that have one. It cannot tell you
that an AR term you \emph{did} fit is poorly estimated, because simulating from
that fit reproduces its own autocorrelation and the discrepancy cancels out.
}

\section{The comparison band}{

Built by refitting simulated data rather than using the usual
\verb{plus or minus 2/sqrt(n)} lines, which assume independent observations and
would over-flag here. Because the statistic takes a maximum across categories,
its reference is calibrated by leave-one-out to avoid an artificial bias
toward significance.
}

\references{
Rue, H., & Held, L. (2005). \emph{Gaussian Markov Random Fields: Theory and
Applications}. Chapman & Hall/CRC. (Background on latent autoregressive
structures.)
}
\seealso{
\code{\link[=lum_appraise]{lum_appraise()}}, \code{\link[=lum_check_omitted]{lum_check_omitted()}}.
}
