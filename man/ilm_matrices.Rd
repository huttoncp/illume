% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_matrices.R, R/zzz-iml-aliases.R
\name{ilm_matrices}
\alias{ilm_matrices}
\alias{iml_matrices}
\title{The design matrices of a fitted model, for new rows}
\usage{
ilm_matrices(object, newdata, time = NULL, group = NULL)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"}.}

\item{newdata}{A data frame of new rows, with the columns the model uses.}

\item{time, group}{For a correlation over time built from vectors, the new
rows' times and groups. Not needed when it was built by name.}
}
\value{
A list with
\describe{
\item{\code{X}}{the fixed design, columns as \code{colnames(object$X)}.}
\item{\code{re}}{one element per grouping term: \code{Z}, its design (the
intercept and slope columns); \code{level}, each row's group label;
\code{group}, its position among the fitted levels, \code{NA} for a new group;
\code{new_group}; and \code{factor}, the grouping variable.}
\item{\code{smooth}}{one penalised basis per smooth term.}
\item{\code{zi}, \code{disp}}{the zero part's and the dispersion model's designs,
when the model has them.}
\item{\code{ar}}{with a correlation over time, a data frame with a row per
new row: \code{group}, \code{time}, \code{cell}, \code{prev_cell}, \code{next_cell},
\code{dt_prev}, \code{dt_next} and \code{new_group}.}
}
}
\description{
Everything needed to assemble a prediction for new rows from the fit's
parameters or from draws of them (\code{\link[=ilm_draws]{ilm_draws()}}): the fixed design, each
random term's design and the group each row belongs to, each smooth's
penalised basis, the zero part's and the dispersion model's designs, and
each row's place among the cells of a correlation over time.
}
\details{
\strong{Groups.} A row's group is matched to the fitted levels by label; a group
the fit has not seen has no code and is marked \code{new_group}.

\strong{Over time.} \code{ar} places each row among its group's cells, as numbered by
\code{\link[=ilm_cells]{ilm_cells()}}: \code{cell} if the row's time is one of them, and the nearest
cells before and after, \code{prev_cell} and \code{next_cell}, with the time from
each, \code{dt_prev} and \code{dt_next}, in the units of the fit's time. A time past
a group's last cell has a \code{prev_cell} and no \code{next_cell} -- the forecast
case. An AR(1) term's grid is fixed, so a time that falls between its steps
is an error rather than rounded to one. The time and the group are read
from the columns the term was built from, \code{ilm_rw1(~ time | group)} and
the like; for a term built from vectors, pass them as \code{time} and \code{group}.
}
\examples{
set.seed(1)
d <- data.frame(id = factor(rep(c("a", "b", "c"), each = 10)), t = rep(1:10, 3))
d$y <- rnorm(30) + rep(cumsum(rnorm(10, 0, 0.5)), 3)
fit <- ilm_model(y ~ 1, data = d, family = "gaussian",
                 ar = ilm_rw1(~ t | id), verbose = FALSE)
nd <- data.frame(t = c(5, 12, 3), id = c("a", "a", "z"))
ilm_matrices(fit, nd)$ar
}
\seealso{
\code{\link[=ilm_draws]{ilm_draws()}}, \code{\link[=ilm_cells]{ilm_cells()}}, \code{\link[=ilm_ranef]{ilm_ranef()}}.
}
