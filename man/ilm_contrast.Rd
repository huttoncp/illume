% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_emmeans.R
\name{ilm_contrast}
\alias{ilm_contrast}
\title{Compare estimated marginal means}
\usage{
ilm_contrast(
  object,
  method = c("pairwise", "trt.vs.ctrl", "poly"),
  ref = NULL,
  adjust = c("max_t", "bonferroni", "none"),
  level = 0.95,
  nsim = 20000L,
  seed = 1L
)
}
\arguments{
\item{object}{An \code{\link[=ilm_emmeans]{ilm_emmeans()}} result.}

\item{method}{\code{"pairwise"}, \code{"trt.vs.ctrl"} or \code{"poly"}. The last assumes
the levels are ordered and equally spaced.}

\item{ref}{Reference level for \code{"trt.vs.ctrl"}.}

\item{adjust}{\code{"max_t"}, \code{"bonferroni"} or \code{"none"}.}

\item{level}{Confidence level for the family.}

\item{nsim}{Draws used to find the studentized-maximum critical value.}

\item{seed}{Random seed for that simulation.}
}
\value{
A data frame with \code{contrast}, \code{estimate}, \code{se}, \code{lower}, \code{upper},
\code{p_value}, \code{p_adj} and \code{adjust}.
}
\description{
Differences between the means from \code{\link[=ilm_emmeans]{ilm_emmeans()}}, with intervals and a
multiplicity adjustment.
}
\section{The adjustment}{


Comparing every pair of five groups is ten tests, and ten intervals each
nominally 95\% do not jointly cover at 95\%. The default is the single-step
studentized maximum: the contrasts' joint covariance is known exactly here,
\verb{C L V L' C'}, so the reference distribution is simulated from it directly
rather than bootstrapped. This is the same construction
\code{\link[=ilm_boot_diff]{ilm_boot_diff()}} uses on raw data, where it reproduced \code{TukeyHSD()} to
0.006 on the design Tukey is exact for.

\code{"bonferroni"} is the conservative fallback and \code{"none"} is there for
comparisons chosen in advance. With a single contrast there is nothing to
adjust and the column reads \code{"none"} whatever was asked.
}

\examples{
set.seed(1); n <- 200
d <- data.frame(g = factor(sample(c("a", "b", "c"), n, TRUE)), x = rnorm(n))
d$y <- 1 + 0.5 * (d$g == "b") + 0.2 * d$x + rnorm(n)
fit <- ilm_model(y ~ g + x, data = d, family = "gaussian", verbose = FALSE)
ilm_contrast(ilm_emmeans(fit, "g"))
}
\seealso{
\code{\link[=ilm_emmeans]{ilm_emmeans()}}, \code{\link[=ilm_boot_diff]{ilm_boot_diff()}} for the same comparison made
without a model.
}
