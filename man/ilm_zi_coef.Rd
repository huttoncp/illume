% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_zi.R, R/zzz-iml-aliases.R
\name{ilm_zi_coef}
\alias{ilm_zi_coef}
\alias{iml_zi_coef}
\title{Coefficients of the zero part of a zero-inflated or hurdle fit}
\usage{
ilm_zi_coef(object, level = 0.95)
}
\arguments{
\item{object}{A fitted \code{\link[=ilm_model]{ilm_model()}} with a \code{ziformula}.}

\item{level}{Confidence level for the interval.}
}
\value{
A data frame of \code{term}, \code{estimate}, \code{se}, \code{lower}, \code{upper}, \code{z} and
\code{p}, with an \code{odds_ratio} column since the scale is a logit.
}
\description{
The second linear predictor a \code{ziformula} adds, on the logit scale: a
positive coefficient means more excess zeros. What "excess" means depends on
which model was fitted -- under \code{zi_type = "inflated"} these are the
structural zeros only, over and above the ones the count part produces by
itself, while under \code{"hurdle"} they are every zero there is.
}
\examples{
set.seed(1); n <- 400
d <- data.frame(x = rnorm(n), z = rnorm(n))
lam <- exp(0.6 + 0.4 * d$x)
d$y <- ifelse(runif(n) < plogis(-0.5 + 0.8 * d$z), 0, rpois(n, lam))
f <- ilm_model(y ~ x, data = d, family = "poisson", ziformula = ~ z,
               verbose = FALSE)
ilm_zi_coef(f)
}
\seealso{
\code{\link[=ilm_zi_prob]{ilm_zi_prob()}} for the fitted probabilities themselves,
\code{\link[=ilm_check_zeros]{ilm_check_zeros()}} for whether a zero part is called for at all.
}
