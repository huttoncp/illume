% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_accessors.R, R/zzz-iml-aliases.R
\name{ilm_ranef}
\alias{ilm_ranef}
\alias{ranef.ilm_model}
\alias{as.data.frame.ilm_ranef}
\alias{iml_ranef}
\title{Random effects of a fitted model}
\usage{
ilm_ranef(object, ...)

\method{ranef}{ilm_model}(object, ...)

\method{as.data.frame}{ilm_ranef}(x, row.names = NULL, optional = FALSE, lme4 = FALSE, ...)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"}.}

\item{...}{Unused.}

\item{row.names, optional}{Unused; for the generic.}

\item{lme4}{Logical. Give \code{lme4}'s long-format names -- \code{grpvar}, \code{term},
\code{grp}, \code{condval}, \code{condsd} -- in place of illume's own.}
}
\value{
A data frame of class \code{"ilm_ranef"}, with columns
\describe{
\item{\code{type}}{\code{"re"}, \code{"smooth"}, or the correlation over time:
\code{"ar1"}, \code{"car1"}, \code{"rw1"}.}
\item{\code{term}}{the term's name in the fit, as in \code{object$Sigma}.}
\item{\code{factor}}{the grouping variable.}
\item{\code{level}}{the group, a factor with the fitted levels; a smooth's
basis functions are \code{b1}, \code{b2}, ...}
\item{\code{dim}}{the coefficient -- \code{"(Intercept)"}, a slope's variable --
prefixed by the category for a multinomial outcome.}
\item{\code{time}, \code{cell}}{for a correlation over time, the cell's time and
its row of \code{\link[=ilm_cells]{ilm_cells()}}; \code{NA} otherwise.}
\item{\code{row}}{the position in \code{object$obj$env$par}.}
\item{\code{mode}, \code{sd}}{the conditional mode and its standard deviation.}
}
}
\description{
The predicted random effects -- the conditional modes -- labelled by the
groups they belong to: one row per level of each grouping factor and each
of its coefficients, one per basis function of a smooth, and one per cell
of a correlation over time.
}
\details{
\code{sd} is each mode's conditional standard deviation with the variance
components at their estimates, from the inner Hessian of the Laplace
approximation: the quantity \code{lme4} calls \code{condVar}. With \code{reml = TRUE} the
fixed effects are integrated out alongside the random ones, so there it
also carries their uncertainty. A fit read back from disk no longer has the
compiled objective, and gives \code{NA}.

\code{row} is each value's position in the fit's full parameter vector,
\code{object$obj$env$par}, which is also the order of its joint precision. Under
REML the fixed effects sit in that vector's random block too; they are not
random effects, and are not listed.

For a reduced-rank term the rows are its latent factor scores; multiplied by
the loadings, \code{object$Lambda}, they give the effect on each category. A
random walk's first cell in each group is held at zero rather than
estimated, so it has no \code{row}, and a mode and \code{sd} of zero.

With \code{nlme} or \code{lme4} attached, \code{ranef(fit)} gives the same.
}
\examples{
set.seed(1)
d <- data.frame(id = factor(rep(sprintf("s\%02d", 1:10), each = 6)),
                x = rnorm(60))
d$y <- 1 + 0.5 * d$x + rnorm(10)[d$id] + rnorm(60)
fit <- ilm_model(y ~ x + (1 | id), data = d, family = "gaussian",
                 verbose = FALSE)
head(ilm_ranef(fit))
}
\seealso{
\code{\link[=ilm_varcorr]{ilm_varcorr()}} for the variance components, \code{\link[=ilm_cells]{ilm_cells()}}.
}
