% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_cor.R, R/zzz-iml-aliases.R
\name{ilm_cells}
\alias{ilm_cells}
\alias{iml_cells}
\title{The cells of a correlation over time}
\usage{
ilm_cells(object)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"}.}
}
\value{
\code{NULL} when the model has no correlation over time. Otherwise a
data frame with one row per cell, ordered by group and then time, and
columns
\describe{
\item{\code{term}}{\code{"ar"}, the name the term has in \code{Sigma} and in the
parameter names.}
\item{\code{type}}{\code{"ar1"}, \code{"car1"} or \code{"rw1"}.}
\item{\code{group}}{the unit, a factor with the fitted levels.}
\item{\code{time}}{the cell's time, on the scale it was given: numeric,
\code{Date} or date-time.}
\item{\code{index}}{where the fit holds the cell's value: the \code{index}-th of
the entries named \code{"B_ar"} in \code{object$sdr$par.random} and in the
joint precision. \code{NA} at a random walk's anchors, which are held at
zero rather than estimated. With a multinomial outcome a cell has
one value per linear predictor, stored predictor after predictor, so
predictor \code{c}'s value is at \code{index + (c - 1) * attr(, "n_latent")}.}
\item{\code{n_obs}}{how many fitted rows sit on the cell; 0 at an AR(1) grid
step no one was observed at.}
\item{\code{anchor}}{\code{TRUE} at each group's first cell of a random walk.}
\item{\code{last}}{\code{TRUE} at each group's final cell, where a forecast
starts. An AR(1) grid runs to the latest time in the data for every
group, so a group seen only early ends in cells without observations,
whose values are the chain's own forecast.}
}
Attributes: \code{obs_cell}, each fitted row's cell (a row number of this
table) in the order of the model frame; \code{vars}, the time and group
columns when the term was given by name, \code{~ time | group}, and \code{NULL}
when it was given vectors; \code{n_latent}, the number of estimated cells;
\code{parameterisation}, what the fitted parameters mean, in words; and for
AR(1), \code{step} and \code{origin}, the grid's spacing and first time.
}
\description{
One row per latent value of a fitted model's \code{\link[=ilm_ar1]{ilm_ar1()}}, \code{\link[=ilm_car1]{ilm_car1()}} or
\code{\link[=ilm_rw1]{ilm_rw1()}} term: which group and time it belongs to, how many observations
sit on it, and where the fit holds it. Code that works with the latent
values -- forecasts, draws, checks -- reads the layout here rather than
rebuilding the index arithmetic, which is how the two come apart.
}
\examples{
set.seed(2)
d <- data.frame(id = rep(c("a", "b"), each = 12), day = rep(1:12, 2))
d$y <- rnorm(24) + rep(cumsum(rnorm(12, 0, 0.5)), 2)
fit <- ilm_model(y ~ 1, data = d, ar = ilm_rw1(~ day | id), verbose = FALSE)
cl <- ilm_cells(fit)
head(cl)
attr(cl, "parameterisation")
}
\seealso{
\code{\link[=ilm_ar1]{ilm_ar1()}}, \code{\link[=ilm_car1]{ilm_car1()}}, \code{\link[=ilm_rw1]{ilm_rw1()}}.
}
