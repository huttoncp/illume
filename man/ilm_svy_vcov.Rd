% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_survey.R, R/zzz-iml-aliases.R
\name{ilm_svy_vcov}
\alias{ilm_svy_vcov}
\alias{iml_svy_vcov}
\title{Linearization variance for a fit from a complex sample}
\usage{
ilm_svy_vcov(object, design = NULL, lonely = c("adjust", "certainty", "fail"))
}
\arguments{
\item{object}{A fitted \code{\link[=ilm_model]{ilm_model()}} with no random effects.}

\item{design}{An \code{\link[=ilm_design]{ilm_design()}}, or \code{NULL} to use the one the fit carries.}

\item{lonely}{What to do with a stratum holding a single PSU, which supplies
no variance of its own. \code{"adjust"} centres it at the grand mean of all PSU
totals instead of its own, which is conservative; \code{"certainty"} treats it
as contributing nothing, which assumes it was selected with certainty;
\code{"fail"} refuses.}
}
\value{
A covariance matrix, with the design degrees of freedom attached.
}
\description{
The design-based (Taylor linearization) covariance of the fixed effects:
score contributions summed within each primary sampling unit, centred within
each stratum, and scaled by that stratum's number of clusters.
}
\section{Why the model-based variance is not an option here}{


A likelihood weighted by sampling weights believes it saw \code{sum(w)}
observations rather than \code{n}, so its standard errors are too small by at
least \code{sqrt(n / sum(w))} -- and by more once the sample is clustered, since
the design effect is on top of that. On the stratified two-stage example in
\code{\link[=ilm_svy_coef]{ilm_svy_coef()}} the weights alone account for a factor of 5.9 and the
observed ratios are 7.3 and 16.6.

The point estimates are unaffected: a weighted likelihood is
design-consistent for the population parameter. That is what makes the
mistake easy to miss -- everything looks right except the uncertainty, and
the uncertainty looks better than right.
}

\references{
Binder, D. A. (1983). On the variances of asymptotically normal
estimators from complex surveys. \emph{International Statistical Review} 51,
279-292.
}
\seealso{
\code{\link[=ilm_svy_coef]{ilm_svy_coef()}}, \code{\link[=ilm_design]{ilm_design()}}.
}
