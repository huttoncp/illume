% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_remedies.R, R/zzz-iml-aliases.R
\name{ilm_apply_remedy}
\alias{ilm_apply_remedy}
\alias{iml_apply_remedy}
\title{Refit a model with one of its remedies}
\usage{
ilm_apply_remedy(object, remedies, which, data = NULL, verbose = FALSE)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object, from the formula interface.}

\item{remedies}{The list from \code{\link[=ilm_remedies]{ilm_remedies()}} for \code{object} that the
remedy was chosen from. Required rather than recomputed, so the remedy
made is always the one that was read -- a list that includes the
standalone checks numbers its remedies differently from one that does
not.}

\item{which}{The \code{id} of the remedy to make.}

\item{data}{The data the model was fitted to, when it cannot be found.}

\item{verbose}{Logical. Print the new fit's checks as it is fitted.}
}
\value{
The refitted model. Its \code{remedy_log} holds every remedy applied
to reach it, in order: the check, status, tier, remedy and change.
}
\description{
Makes one remedy from \code{\link[=ilm_remedies]{ilm_remedies()}}: refits the model through its own
call with only that change, so everything else about it -- the family, the
weights, a zero part, a dispersion model, REML -- comes along, and the new
fit's call is the one a person would have written, ready for \code{\link[=update]{update()}} or
a further remedy. Says what the checks that called for the remedy say now,
and which checks are not OK after the refit.
}
\details{
The data are found where the model's formula was created, as \code{update()}
would; pass \code{data} when they are not there, e.g. when the model was fitted
inside a function from a formula written outside it.
}
\examples{
set.seed(1)
d <- data.frame(g = factor(rep(1:20, each = 10)), x = rnorm(200), e = rnorm(200))
d$x <- d$x - ave(d$x, d$g)
d$y <- 1 + 0.5 * d$x + d$e - ave(d$e, d$g)   # no group effect at all
f <- ilm_model(y ~ x + (1 | g), data = d, verbose = FALSE)
rem <- ilm_remedies(f)
rem
f2 <- ilm_apply_remedy(f, rem, 1)
f2$remedy_log
}
\seealso{
\code{\link[=ilm_remedies]{ilm_remedies()}}.
}
