% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_ordinal.R
\name{ilm_thresholds}
\alias{ilm_thresholds}
\title{Thresholds of a cumulative link fit}
\usage{
ilm_thresholds(object, level = 0.95)
}
\arguments{
\item{object}{A fitted \code{\link[=ilm_model]{ilm_model()}} with an ordinal family.}

\item{level}{Confidence level.}
}
\value{
A data frame of \code{cut}, \code{estimate}, \code{se}, \code{lower} and \code{upper}.
}
\description{
The cut points on the latent scale, with their standard errors. There are
\code{J - 1} of them for \code{J} categories and they are increasing by construction:
the fit estimates the first and the logarithms of the gaps, so an ordering
violation is not something the optimiser can reach.
}
\details{
Read them against the linear predictor, which is \strong{subtracted}:
\code{P(Y <= j) = F(threshold_j - eta)}. A positive coefficient therefore pushes
probability towards the higher categories.
}
\examples{
set.seed(1); n <- 400
d <- data.frame(x = rnorm(n))
z <- 0.8 * d$x + rlogis(n)
d$y <- factor(cut(z, c(-Inf, -1, 1, Inf), labels = c("lo", "mid", "hi")),
              ordered = TRUE)
f <- ilm_model(y ~ x, data = d, family = "ordinal", verbose = FALSE)
ilm_thresholds(f)
}
\seealso{
\code{\link[=ilm_check_proportional]{ilm_check_proportional()}} for the assumption that makes one
coefficient per predictor enough.
}
