% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_draws.R, R/zzz-iml-aliases.R
\name{ilm_draws}
\alias{ilm_draws}
\alias{iml_draws}
\title{Joint draws of every parameter of a fitted model}
\usage{
ilm_draws(
  object,
  nsim = 1000L,
  seed = NULL,
  given = c("none", "theta", "parameters"),
  blocks = NULL,
  natural = TRUE
)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"}.}

\item{nsim}{Number of draws.}

\item{seed}{Optional random seed.}

\item{given}{\code{"none"} to draw everything, \code{"theta"} to hold the variance
parameters at their estimates, or \code{"parameters"} to hold every parameter
and draw only the random effects and the cells.}

\item{blocks}{Optional character vector of the blocks to return, named as in
\code{names(object$obj$env$par)}: \code{"beta"}, \code{"theta"}, \code{"bvec"}, \code{"logdisp"},
\code{"B_ar"}, \code{"lchol_ar"}, \code{"rho_raw"} and so on.}

\item{natural}{Logical. Also transform each draw's variance parameters to
the natural scale, as \code{\link[=ilm_varcorr]{ilm_varcorr()}} reports them.}
}
\value{
An object of class \code{"ilm_draws"}, a list with
\describe{
\item{\code{draws}}{a matrix, one row per parameter and one column per draw,
on the fit's internal scale (log standard deviations and the like),
with rows named by block.}
\item{\code{mode}}{the centre: the estimates and conditional modes.}
\item{\code{map}}{a data frame saying what each row is: \code{row}, \code{block},
\code{term} (a coefficient's or parameter's name, or a random term's), and
for a random effect its \code{level} (the group's label), \code{dim} and, for a
correlation over time, \code{cell} (the row of \code{\link[=ilm_cells]{ilm_cells()}}).}
\item{\code{natural}}{with \code{natural = TRUE}, each draw's variance
parameters on their natural scale: \code{re}, one array per random term of
its covariance by draw; \code{latent}, a correlation over time's
covariance by draw with its \code{rho} (and \code{range}) or, for a random walk,
\code{var_per_time}; and \code{dispersion}, one row per dispersion parameter.
At the estimate these are exactly \code{\link[=ilm_varcorr]{ilm_varcorr()}}'s.}
\item{\code{held}}{the number of directions held at a boundary, and the
terms they belong to; none under \code{given = "theta"} or
\code{"parameters"}, which hold the variance parameters whole.}
\item{\code{given}}{as asked.}
}
}
\description{
Draws the fixed effects, the variance and dispersion parameters, the random
effects and the cells of a correlation over time together, from the
Gaussian approximation to their joint distribution that the fit's standard
errors come from.
}
\details{
\strong{The distribution.} The draws are centred on the estimates and the
conditional modes, with covariance the inverse of the joint precision --
the Laplace approximation's curvature in every parameter at once. For the
fixed parameters that is exactly \code{vcov(fit, full = TRUE)}; for the random
effects it adds the uncertainty the fixed parameters bring to the
conditional SDs of \code{\link[=ilm_ranef]{ilm_ranef()}}.

\strong{At a boundary.} When a random term's covariance sits at the edge of its
range, the fit holds the direction the data cannot resolve at its estimate
(see \code{fit$hessian_held}), and so do the draws: exactly, by conditioning the
joint distribution on those directions. \code{held} says how many.

\strong{\code{given = "theta"}} holds the variance parameters of the random terms and
of a correlation over time at their estimates, and draws everything else
from its distribution given them -- the approach of the \code{merTools}
package. The dispersion and the family's other parameters are still drawn.

\strong{\code{given = "parameters"}} holds every parameter at its estimate -- the
fixed effects, the variance, dispersion and correlation parameters, and the
family's others -- and draws only the random effects and the cells of a
correlation over time, from their distribution given the parameters: the
curvature of the fit's inner optimisation at the conditional modes, which
for a gaussian model is that distribution exactly. Their SDs are
\code{\link[=ilm_ranef]{ilm_ranef()}}'s \code{sd}, except under REML, where \code{ilm_ranef()} integrates
over the fixed effects and these hold them, so these are smaller. The
variance components in \code{natural} are then those at the estimate.

\strong{\code{blocks}} returns only the named blocks of the parameter vector -- say
\code{c("beta", "bvec")} -- which saves memory; the draw is joint either way.

A fit made without \code{joint = TRUE} has no joint precision stored, and it is
formed here from the fit's compiled objective; a fit read back from disk no
longer has that, and has to be refitted.
}
\examples{
set.seed(1)
d <- data.frame(id = factor(rep(1:12, each = 6)), x = rnorm(72))
d$y <- 1 + 0.5 * d$x + rnorm(12)[d$id] + rnorm(72)
fit <- ilm_model(y ~ x + (1 | id), data = d, family = "gaussian",
                 verbose = FALSE)
dr <- ilm_draws(fit, nsim = 200, seed = 1)
dim(dr$draws)
head(dr$map)
}
\seealso{
\code{\link[=ilm_ranef]{ilm_ranef()}}, whose \code{row} indexes these draws' full layout;
\code{\link[=ilm_varcorr]{ilm_varcorr()}}; \code{\link[=ilm_cells]{ilm_cells()}}.
}
