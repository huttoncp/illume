% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_check.R
\name{ilm_check_predictive}
\alias{ilm_check_predictive}
\title{Compare the observed response with data the model would generate}
\usage{
ilm_check_predictive(object, B = 50L, seed = 1L, plot = TRUE)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{B}{Simulated datasets.}

\item{seed}{Random seed.}

\item{plot}{Draw the overlay.}
}
\value{
Invisibly, a list with the observed and simulated summaries and a
\code{status}.
}
\description{
The frequentist counterpart of a posterior predictive check: simulate
datasets from the fitted model and see whether the real response looks like
one of them. It is a blunt instrument, and that is the point -- it catches
gross misspecification (the wrong family, unmodelled zero inflation, a
bounded response fitted as gaussian) at a glance, where a residual plot
makes you work for it.
}
\examples{
set.seed(1)
d <- ilm_sim()
f <- ilm_model(visits ~ income + (1 | id), data = d, family = "poisson",
               verbose = FALSE)
ilm_check_predictive(f, B = 30)
}
\seealso{
\code{\link[=ilm_check_dispersion]{ilm_check_dispersion()}}, \code{\link[=ilm_check_zeros]{ilm_check_zeros()}} for the specific
failures this can only hint at.
}
