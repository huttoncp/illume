% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_variogram.R
\name{ilm_variogram}
\alias{ilm_variogram}
\title{Residual correlation as a function of separation in time}
\usage{
ilm_variogram(
  object,
  time,
  group,
  coords = NULL,
  breaks = 8L,
  B = 100L,
  type = "correlation",
  min_effect = 0.1,
  ncores = 1L,
  seed = 1L,
  max_pairs = 2e+05,
  plot = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{time}{Time, one value per observation. Give this or \code{coords}.}

\item{group}{Grouping variable, one value per observation. Pairs are only
formed within a group. Optional for a spatial variogram, where the default
treats every observation as comparable with every other.}

\item{coords}{Spatial coordinates, one to three columns, for a variogram
over distance rather than over time.}

\item{breaks}{Number of separation bins, or explicit break points. Bins are
chosen to hold roughly equal numbers of pairs, so a bin far out is not
estimated from a handful of them.}

\item{B}{Simulated datasets behind the envelope. A p-value cannot fall below
\code{1/(B+1)}, so about 100 is needed before \code{FAIL} is reachable.}

\item{type}{\code{"correlation"} or \code{"semivariance"}.}

\item{min_effect}{Smallest departure from the simulated null worth a
verdict. A bin outside the envelope by less than this is reported as \code{OK}.
Each bin rests on thousands of pairs, so the envelope narrows until any
imperfection clears it, and significance stops being the same thing as
something to act on. Set to \code{0} to flag on the envelope alone.}

\item{ncores}{Worker processes for the refits.}

\item{seed}{Random seed.}

\item{max_pairs}{Cap on the number of within-group pairs used. A group of
\code{m} observations contributes \code{m(m-1)/2} of them, so a few long series can
run to millions; beyond the cap a random subset is taken, which changes the
precision but not what is being estimated.}

\item{plot}{Draw the variogram.}

\item{verbose}{Print the table.}
}
\value{
Invisibly, a list with the per-bin \code{table}, the simulated null, and
the number of replicates that refitted.
}
\description{
Bins every pair of observations within a group by how far apart they are, and
reports the residual correlation in each bin against an envelope built by
simulating from the fitted model and refitting. This is the check for a
\code{\link[=ilm_car1]{ilm_car1()}} term, and the one to use whenever observation times are
irregular.
}
\section{Why not \code{\link[=ilm_check_ar]{ilm_check_ar()}}}{

That function matches pairs at exact lags, which is right for evenly spaced
data and close to useless without it. On a 60-unit panel with six
observations each, drawn from thirty possible times, exact matching found 53
pairs at lag 1, five at lag 2 and none beyond -- five of six lags returned no
verdict. Binning by distance uses all 900 pairs.
}

\section{Reading it}{

Correlation that starts high and decays toward zero as separation grows is
what an autoregressive process looks like, and is what \code{\link[=ilm_car1]{ilm_car1()}} fits.
Correlation that is flat and positive at every separation is a group effect
the random intercept has not absorbed. Correlation that rises again at some
separation is a cycle; see \code{\link[=ilm_fourier]{ilm_fourier()}}.

The envelope is not centred on zero, and should not be. Residuals within a
group sum to roughly zero, so pairs of them correlate negatively even when
the model is exactly right, and the further apart they are the more strongly
this bites.
}

\section{Relation to the semivariogram}{

For standardised residuals the semivariance at separation \code{d} is \code{1 - r(d)},
so \code{type = "semivariance"} plots the same information the other way up, in
the form \code{nlme::Variogram()} uses.
}

\examples{
set.seed(1)
d <- data.frame(id = factor(rep(1:20, each = 5)),
                t = as.vector(replicate(20, sort(sample(1:20, 5)))),
                x = rnorm(100))
d$y <- d$x + rnorm(100)
f <- ilm_model(y ~ x + (1 | id), data = d, family = "gaussian",
               verbose = FALSE)
ilm_variogram(f, d$t, d$id, breaks = 4, B = 15, plot = FALSE)
}
\references{
Pinheiro, J. C., & Bates, D. M. (2000). \emph{Mixed-Effects Models in S and
S-PLUS}. Springer. (Chapter 5 covers the residual variogram.)
}
\seealso{
\code{\link[=ilm_car1]{ilm_car1()}} for the remedy, \code{\link[=ilm_check_ar]{ilm_check_ar()}} for evenly spaced
data, \code{\link[=ilm_plot_acf]{ilm_plot_acf()}}.
}
