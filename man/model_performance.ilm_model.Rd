% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_ic.R
\name{model_performance.ilm_model}
\alias{model_performance.ilm_model}
\title{Fit indices for a fitted model}
\usage{
\method{model_performance}{ilm_model}(model, metrics = "all", ..., verbose = TRUE)
}
\arguments{
\item{model}{A fitted \code{"ilm_model"} object.}

\item{metrics}{Unused; present for compatibility with the generic.}

\item{...}{Unused.}

\item{verbose}{Logical. Message when the fit failed its checks.}
}
\value{
A one-row data frame of class \code{"performance_model"}.
}
\description{
A \code{performance::model_performance()} method. Returns AIC, AICc, BIC (both
sample-size conventions), the log-likelihood, degrees of freedom, McFadden's
R-squared, and the scoring rules from \code{\link[=ilm_scores]{ilm_scores()}}.
}
\section{What is deliberately absent}{

Nakagawa's marginal and conditional R-squared, and the ICC, are \strong{not}
reported. Both require a distribution-specific residual variance, and a
nominal multinomial outcome does not have one -- reporting them would mean
inventing a quantity rather than estimating it -- so they are left out for
every family rather than given for some.

McFadden's R-squared is relative to an intercept-only model with the same
grouping random effects, so it answers "how much do the predictors add?"
rather than "how much does the whole model explain?". Unlike R-squared in
linear regression, values around 0.2 to 0.4 already indicate a good fit.

The scoring rules are computed \strong{in sample} and conditional on the fitted
random effects, so both are optimistic. Use cross-validation or a held-out
set if you need an honest predictive comparison.
}

\references{
McFadden, D. (1974). Conditional logit analysis of qualitative choice
behavior. In P. Zarembka (Ed.), \emph{Frontiers in Econometrics}. Academic Press.

Nakagawa, S., & Schielzeth, H. (2013). A general and simple method for
obtaining R-squared from generalized linear mixed-effects models. \emph{Methods in
Ecology and Evolution}, 4(2), 133--142. (The approach not used here, and why:
it needs a distribution-specific variance.)
}
