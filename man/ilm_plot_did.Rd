% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_did.R, R/zzz-iml-aliases.R
\name{ilm_plot_did}
\alias{ilm_plot_did}
\alias{iml_plot_did}
\title{Event-study plot for a difference in differences}
\usage{
ilm_plot_did(
  x,
  main = "Event study",
  xlab = "periods from treatment",
  ylab = "effect",
  ...
)
}
\arguments{
\item{x}{An \code{\link[=ilm_did]{ilm_did()}} result.}

\item{main, xlab, ylab}{Labels.}

\item{...}{Passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
One coefficient per period relative to treatment, with intervals. The
pre-treatment coefficients are the diagnostic: under parallel trends they sit
near zero, and the shape of any departure says more than a single test does.
}
\examples{
set.seed(1)
d <- expand.grid(unit = 1:40, time = 1:8)
d$treated <- as.integer(d$unit <= 20)
d$post <- as.integer(d$time >= 5)
d$y <- 1 + 0.3 * d$time + rnorm(40)[d$unit] +
       0.8 * d$treated * d$post + rnorm(nrow(d))
fit <- ilm_did(d, "y", "unit", "time", treated = "treated", post = "post",
               verbose = FALSE)
ilm_plot_did(fit)
}
\seealso{
\code{\link[=ilm_did]{ilm_did()}}.
}
