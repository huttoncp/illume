% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lum_model.R
\name{terms.lum_model}
\alias{terms.lum_model}
\alias{formula.lum_model}
\alias{model.frame.lum_model}
\alias{model.matrix.lum_model}
\alias{getCall.lum_model}
\title{Standard accessors for a fitted model}
\usage{
\method{terms}{lum_model}(x, ...)

\method{formula}{lum_model}(x, ...)

\method{model.frame}{lum_model}(formula, ...)

\method{model.matrix}{lum_model}(object, ...)

\method{getCall}{lum_model}(x, ...)
}
\arguments{
\item{x, object, formula}{A fitted \code{"lum_model"} object.}

\item{...}{Unused.}
}
\value{
The model terms, formula, model frame, model matrix or original call.

The model terms, formula, model frame, model matrix or original call.

The model terms, formula, model frame, model matrix or original call.

The model terms, formula, model frame, model matrix or original call.

The model terms, formula, model frame, model matrix or original call.
}
\description{
Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.

Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.

Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.

Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.

Supply the pieces that other packages expect from a model object, so that
\code{car::Anova()}, \code{emmeans} and \code{marginaleffects} can work with a fit.
}
