% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_plot_acf.R, R/zzz-iml-aliases.R
\name{ilm_plot_acf}
\alias{ilm_plot_acf}
\alias{iml_plot_acf}
\title{Autocorrelation and partial-autocorrelation plots with verdicts}
\usage{
ilm_plot_acf(
  object,
  time,
  group,
  maxlag = 8L,
  B = 100L,
  which = "both",
  ncores = 1L,
  seed = 1L,
  colour = "grey25",
  fill = "grey85",
  alpha = NULL,
  size = 1,
  main = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"}, or the value returned by
\code{\link[=ilm_check_ar]{ilm_check_ar()}}, which lets the plot reuse that call's refits instead of
paying for them twice.}

\item{time}{Integer time index, one per observation. Ignored when \code{object}
already carries an envelope.}

\item{group}{Grouping variable, one per observation.}

\item{maxlag}{Integer. Largest lag to examine.}

\item{B}{Integer. Simulated datasets behind the envelope. No p-value can
fall below \code{1/(B+1)}, so roughly 100 is needed before \code{FAIL} is reachable.}

\item{which}{\code{"both"}, \code{"acf"} or \code{"pacf"}.}

\item{ncores}{Integer. Worker processes for the refits.}

\item{seed}{Integer. Random seed.}

\item{colour}{Colour for lags that are not flagged.}

\item{fill}{Band fill colour.}

\item{alpha}{Band transparency, in the ggplot2 sense.}

\item{size}{Scaling for points and spikes.}

\item{main}{Title. With \code{which = "both"} it is used for the first panel.}

\item{verbose}{Logical. Also print the per-lag table.}

\item{...}{Unused, present so the \code{plot()} method can pass through.}
}
\value{
Invisibly, the \code{"ilm_ar_envelope"} object behind the plot, which can
be passed straight back in to redraw without refitting.
}
\description{
Draws the residual autocorrelation of a fitted model, within group and by
lag, against an envelope built by simulating from the fit and refitting.
Lags the check flags are coloured, and the panel is annotated with what the
pattern means and what to do about it.
}
\section{Why not \code{acf()}}{

\code{stats::acf()} treats its input as one series and draws a
\verb{plus or minus 2/sqrt(n)} band. Neither is right here. Panel data stacked
long is many short series, and pairing across the joins manufactures
correlation that is not there; and the \code{2/sqrt(n)} band assumes independent
observations from an exactly-correct model, which in-sample residuals from a
mixed model are not. Both problems push in the same direction, toward
flagging autocorrelation that does not exist.
}

\section{Reading the two panels together}{

For an autoregressive process of order p the partial autocorrelation cuts off
after lag p while the ordinary one decays geometrically; for a moving-average
process the ordinary one cuts off and the partial one decays. So the partial
panel is the one that tells you the \emph{order}, which is what decides whether an
AR(1) term is enough.
}

\section{What the colours mean}{

The band is drawn at the critical value the p-value is computed against, so a
point outside the band is exactly a lag the check flags. Flagged lags are
coloured by verdict -- amber for \code{WARN}, red for \code{FAIL} -- regardless of
\code{colour}, because there the colour \emph{is} the verdict. Lags with too few
usable pairs to estimate are marked with a cross on the zero line rather than
silently omitted.
}

\examples{
set.seed(1)
d <- ilm_sim(n_id = 20, n_period = 8)
f <- ilm_model(score ~ income + (1 | id), data = d, family = "gaussian",
               verbose = FALSE)
ilm_plot_acf(f, time = as.integer(factor(d$date)), group = d$id,
             maxlag = 3, B = 12)
}
\seealso{
\code{\link[=ilm_check_ar]{ilm_check_ar()}} for the table and the p-values,
\code{\link[=ilm_check_variance]{ilm_check_variance()}} for the other within-group assumption.
}
