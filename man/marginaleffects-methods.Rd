% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_marginaleffects.R
\name{get_coef.ilm_model}
\alias{get_coef.ilm_model}
\alias{set_coef.ilm_model}
\alias{get_vcov.ilm_model}
\alias{get_predict.ilm_model}
\title{marginaleffects interface}
\usage{
get_coef.ilm_model(model, ...)

set_coef.ilm_model(model, coefs, ...)

get_vcov.ilm_model(model, vcov = NULL, ...)

get_predict.ilm_model(
  model,
  newdata = NULL,
  type = "response",
  marginal = NULL,
  ndraw = 100L,
  ...
)
}
\arguments{
\item{model}{A fitted \code{"ilm_model"} object.}

\item{...}{Unused.}

\item{coefs}{Numeric replacement parameter vector.}

\item{vcov}{Passed through by \code{marginaleffects}.}

\item{newdata}{Optional data frame.}

\item{type}{Prediction type.}

\item{marginal}{Logical. Population-averaged predictions; defaults to the
\code{ilm_model.marginal} option, which is \code{TRUE}.}

\item{ndraw}{Integer. Random-effect draws when \code{marginal = TRUE}.}
}
\value{
Parameters, a covariance matrix, a modified model, or a long-format
data frame of predictions with \code{rowid}, \code{group} and \code{estimate}.

Parameters, a covariance matrix, a modified model, or a long-format
data frame of predictions with \code{rowid}, \code{group} and \code{estimate}.

Parameters, a covariance matrix, a modified model, or a long-format
data frame of predictions with \code{rowid}, \code{group} and \code{estimate}.

Parameters, a covariance matrix, a modified model, or a long-format
data frame of predictions with \code{rowid}, \code{group} and \code{estimate}.
}
\description{
The four methods \code{marginaleffects} needs. It computes derivatives
\strong{numerically}: it asks for the parameters, nudges them, sets them back, and
re-predicts.

The four methods \code{marginaleffects} needs. It computes derivatives
\strong{numerically}: it asks for the parameters, nudges them, sets them back, and
re-predicts.

The four methods \code{marginaleffects} needs. It computes derivatives
\strong{numerically}: it asks for the parameters, nudges them, sets them back, and
re-predicts.

The four methods \code{marginaleffects} needs. It computes derivatives
\strong{numerically}: it asks for the parameters, nudges them, sets them back, and
re-predicts.
}
\details{
\code{get_coef()} returns fixed effects \strong{and} covariance parameters together,
because a population-averaged prediction depends on both. Returning only the
fixed effects would silently report no uncertainty from the covariance
parameters. Variance parameters therefore appear among the "coefficients" in
printed output, which looks odd but is correct.

\code{get_coef()} returns fixed effects \strong{and} covariance parameters together,
because a population-averaged prediction depends on both. Returning only the
fixed effects would silently report no uncertainty from the covariance
parameters. Variance parameters therefore appear among the "coefficients" in
printed output, which looks odd but is correct.

\code{get_coef()} returns fixed effects \strong{and} covariance parameters together,
because a population-averaged prediction depends on both. Returning only the
fixed effects would silently report no uncertainty from the covariance
parameters. Variance parameters therefore appear among the "coefficients" in
printed output, which looks odd but is correct.

\code{get_coef()} returns fixed effects \strong{and} covariance parameters together,
because a population-averaged prediction depends on both. Returning only the
fixed effects would silently report no uncertainty from the covariance
parameters. Variance parameters therefore appear among the "coefficients" in
printed output, which looks odd but is correct.
}
\references{
Arel-Bundock, V., Greifer, N., & Heiss, A. (2024). How to interpret
statistical models using marginaleffects for R and Python. \emph{Journal of
Statistical Software}, 111(9), 1--32.

Arel-Bundock, V., Greifer, N., & Heiss, A. (2024). How to interpret
statistical models using marginaleffects for R and Python. \emph{Journal of
Statistical Software}, 111(9), 1--32.

Arel-Bundock, V., Greifer, N., & Heiss, A. (2024). How to interpret
statistical models using marginaleffects for R and Python. \emph{Journal of
Statistical Software}, 111(9), 1--32.

Arel-Bundock, V., Greifer, N., & Heiss, A. (2024). How to interpret
statistical models using marginaleffects for R and Python. \emph{Journal of
Statistical Software}, 111(9), 1--32.
}
