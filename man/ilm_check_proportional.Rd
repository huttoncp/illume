% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_ordinal.R, R/zzz-iml-aliases.R
\name{ilm_check_proportional}
\alias{ilm_check_proportional}
\alias{iml_check_proportional}
\title{Does one coefficient per predictor describe every cut?}
\usage{
ilm_check_proportional(
  object,
  B = 199L,
  alpha = 0.05,
  seed = 1L,
  progress = NULL
)
}
\arguments{
\item{object}{A fitted \code{\link[=ilm_model]{ilm_model()}} with an ordinal family.}

\item{B}{Simulated datasets for the reference. A simulated p-value cannot
fall below \code{1 / (B + 1)}.}

\item{alpha}{Level for the per-term verdicts.}

\item{seed}{Random seed.}

\item{progress}{Show a progress bar; see \link{ilm_progress_arg}.}
}
\value{
Invisibly, a data frame with one row per fixed-effect term giving
the spread statistic, a simulated p-value, the largest gap between cuts
and a \code{status}, with the per-cut coefficients in a \code{coefs} attribute.
}
\description{
A cumulative link model says a predictor shifts all \code{J - 1} cut points by
the same amount. Under a logit link that is the proportional-odds
assumption, and it is what makes the model economical -- and wrong, when it
does not hold, in a way that is invisible in the coefficient table.
}
\details{
This is the Brant idea, done by refitting rather than by a score test. Each
cut becomes a binary outcome (\code{y > j}), a separate model is fitted to each,
and the statistic is how far the resulting coefficients spread apart
relative to their standard errors.
}
\section{Why the reference is simulated}{


Those separate fits share their data, so their estimates are correlated,
and the spread statistic is not chi-squared however tempting the shape.
Treating it as one was measured here at a \strong{31.5\% false-alarm rate} for a
binary predictor whose effect was perfectly proportional -- with two cuts
the statistic is \code{(b1 - b2)^2} over an average variance, where the honest
denominator is \verb{v1 + v2 - 2c}, and the covariance \code{c} that is missing
depends on the predictor.

The reference is therefore simulated from the fitted proportional-odds
model, which carries that correlation because it arises from the same
refitting. Over 400 replicates per design, with the assumption holding:

\if{html}{\out{<div class="sourceCode">}}\preformatted{                   flagged at 0.05        p-values under the null
  n = 400   x 0.032, g 0.040          p<.05  0.042 / 0.056
  n = 1500  x 0.052, g 0.072          p<.25  0.255 / 0.244
                                      p<.50  0.510 / 0.484
}\if{html}{\out{</div>}}

and with \code{x} acting on the first cut only, the offending term is flagged in
400 of 400 samples at both sizes, while the term that does act
proportionally is flagged 1.5\% and 2.8\% of the time -- it localises the
violation rather than condemning the model. The 7.2\% at the largest size is
a little above nominal and is reported here rather than rounded away.
}

\section{What to do when it fails}{


The remedy is \code{family = "multinomial"}, which spends \code{J - 1} coefficients
per predictor and assumes nothing about the ordering. It nests this model,
so \code{\link[=AIC]{AIC()}} will say whether the extra parameters earn their place. When
only one or two terms offend, that is the trade to weigh: the ordering is
real information, and giving all of it up to accommodate one predictor may
cost more than it buys.
}

\examples{
set.seed(1); n <- 500
d <- data.frame(x = rnorm(n))
z <- 0.8 * d$x + rlogis(n)
d$y <- factor(cut(z, c(-Inf, -1, 1, Inf), labels = c("lo", "mid", "hi")),
              ordered = TRUE)
f <- ilm_model(y ~ x, data = d, family = "ordinal", verbose = FALSE)
ilm_check_proportional(f, B = 99)
}
\references{
Brant, R. (1990). Assessing proportionality in the proportional
odds model for ordinal logistic regression. \emph{Biometrics} 46, 1171-1178.
}
\seealso{
\code{\link[=ilm_thresholds]{ilm_thresholds()}}, \code{\link[=ilm_model]{ilm_model()}}.
}
