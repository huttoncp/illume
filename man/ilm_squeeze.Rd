% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_beta.R, R/zzz-iml-aliases.R
\name{ilm_squeeze}
\alias{ilm_squeeze}
\alias{iml_squeeze}
\title{Move proportions off 0 and 1}
\usage{
ilm_squeeze(y, n = length(y), quiet = FALSE)
}
\arguments{
\item{y}{A numeric vector of proportions in \verb{[0, 1]}.}

\item{n}{Sample size to use in the transformation; the number of
observations by default.}

\item{quiet}{Suppress the message reporting the shift.}
}
\value{
A numeric vector strictly inside \verb{(0, 1)}.
}
\description{
The beta family is defined on the open interval, so a proportion recorded as
exactly 0 or 1 has no likelihood. When those values are a rounding of
interior ones rather than a separate process, the usual remedy is the
transformation of Smithson and Verkuilen (2006):
}
\details{
\deqn{y' = \frac{y (n - 1) + 1/2}{n}}

which compresses the whole scale towards one half by a factor of
\code{(n - 1) / n}. Every value moves, not only the offending ones: nudging the
endpoints alone would leave a gap just inside them and change the shape of
the distribution more than this does.

It is a choice, not a repair. The function reports how far it moved things
so the choice is visible, and with \code{n} small the shift is not negligible --
at \code{n = 50} an observed 0 becomes 0.01.
}
\examples{
y <- c(0, 0.2, 0.5, 0.9, 1)
ilm_squeeze(y)
}
\references{
Smithson, M. and Verkuilen, J. (2006). A better lemon squeezer?
Maximum-likelihood regression with beta-distributed dependent variables.
\emph{Psychological Methods} 11, 54-71.
}
\seealso{
\code{\link[=ilm_model]{ilm_model()}} with \code{family = "beta"}, and \code{ziformula} for the case
where the boundary values are a separate process rather than rounding.
}
