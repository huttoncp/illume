% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_boot.R
\name{ilm_boot_diff}
\alias{ilm_boot_diff}
\title{Bootstrap interval for a difference between two groups}
\usage{
ilm_boot_diff(
  data,
  y,
  group,
  stat = "mean",
  R = 2000L,
  conf = 0.95,
  ci_type = "percentile",
  seed = NULL
)
}
\arguments{
\item{data}{A data frame, or a numeric vector when \code{y} is \code{NULL}.}

\item{y}{Name of the numeric column.}

\item{group}{Name of a column with exactly two levels.}

\item{stat}{\code{"mean"}, \code{"median"}, \code{"sd"}, \code{"var"}, or a function taking a
numeric vector.}

\item{R}{Bootstrap replicates.}

\item{conf}{Confidence level.}

\item{ci_type}{\code{"percentile"}, \code{"bca"}, \code{"normal"} or \code{"basic"}. On skewed
data percentile and BCa hold their nominal coverage better than the other
two.}

\item{seed}{Random seed.}
}
\value{
A one-row data frame with \code{observed}, \code{lower}, \code{upper} and
\code{excludes_zero}.
}
\description{
Reports the difference itself with its own interval, rather than two
intervals to compare by eye: judging a difference by whether separate
intervals overlap is conservative and lossy.
}
\examples{
d <- ilm_sim()
d2 <- d[d$grp \%in\% c("alpha", "beta"), ]
d2$grp <- factor(d2$grp)
ilm_boot_diff(d2, "score", "grp", R = 200, seed = 1)
}
