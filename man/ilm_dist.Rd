% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_dist.R, R/zzz-iml-aliases.R
\name{ilm_dist}
\alias{ilm_dist}
\alias{iml_dist}
\title{A fitted model's response distribution, as functions}
\usage{
ilm_dist(object)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"}.}
}
\value{
An object of class \code{"ilm_dist"}: a list with the functions
\code{d(y, lp, par, log = FALSE, status = NULL, size = 1)},
\code{p(q, lp, par, lower.tail = TRUE, size = 1)} (\code{NULL} for a multinomial
response), \code{q(prob, lp, par, size = 1)} (likewise), \code{r(lp, par, size = 1)}
and \code{mean(lp, par, size = 1)}, and \code{family}, \code{discrete}, \code{zero} (the kind
of zero part, or \code{NULL}), \code{categories}, \code{lp_names} and \code{par_names}.
}
\description{
The density, distribution function, quantile function, random generator
and mean of a fit's response, in illume's own parameterisation, taking
linear predictors and internal parameters -- a draw from \code{\link[=ilm_draws]{ilm_draws()}}, say
-- rather than means.
}
\details{
\strong{Inputs.} \code{lp} is a named list of linear predictors on the link scale:
\code{mu}, the mean's (an \code{n}-by-\code{C} matrix for a multinomial response); \code{zi},
the zero part's, for a zero-inflated or hurdle model; and \code{disp}, the
dispersion model's, for a model with \code{dispformula} -- its data columns
only, since the dispersion's power of the mean is added here from
\code{par["mu_pow"]}. \code{par} holds the family's own parameters, named as the
blocks of \code{\link[=ilm_draws]{ilm_draws()}}: \code{logdisp} (the log dispersion), \code{zeta_raw} (an
ordered response's thresholds, as fitted: a first value and log
increments) and \code{mu_pow}. Each entry of \code{lp} and \code{par} may have one value
or one per element, so a vector of rows by draws is scored in one call.

\strong{Censoring.} \code{status} is \code{-1} for a value known only to be at or below
\code{y}, \code{1} for one at or above it, and \code{0} for an observed one, as
\code{\link[=ilm_censor]{ilm_censor()}} codes them; a censored row's density is the probability of
its interval.

\strong{A binomial response} is a proportion, with \code{size} its number of trials.
The density then includes the binomial coefficient, which the fit's
likelihood leaves out (see \code{\link[=logLik.ilm_model]{logLik.ilm_model()}}).

\strong{Categories.} For an ordered or multinomial response \code{y} is a category:
its position among \code{categories}, or its label. \code{mean()} gives each
category's probability. A multinomial response has no ordering, so no
distribution or quantile function.

The flexible parametric survival families are not covered: their linear
predictor is a function of time itself; see \code{\link[=ilm_survival]{ilm_survival()}}.
}
\examples{
set.seed(1)
d <- data.frame(x = rnorm(200))
d$y <- rpois(200, exp(0.5 + 0.4 * d$x))
fit <- ilm_model(y ~ x, data = d, family = "poisson", verbose = FALSE)
dd <- ilm_dist(fit)
lp <- list(mu = as.numeric(fit$X \%*\% fit$beta))
## the fit's log-likelihood, row by row
sum(dd$d(d$y, lp, par = NULL, log = TRUE))
logLik(fit)
}
\seealso{
\code{\link[=ilm_draws]{ilm_draws()}}, \code{\link[=ilm_matrices]{ilm_matrices()}}.
}
