% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_df.R, R/zzz-iml-aliases.R
\name{ilm_denom_df}
\alias{ilm_denom_df}
\alias{iml_denom_df}
\title{Denominator degrees of freedom for one or more contrasts}
\usage{
ilm_denom_df(
  object,
  L,
  method = c("auto", "satterthwaite", "kenward-roger", "residual", "asymptotic"),
  h = 1e-05
)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{L}{A contrast vector of length \code{p}, or a \code{q}-by-\code{p} contrast matrix.}

\item{method}{One of \code{"auto"}, \code{"satterthwaite"}, \code{"kenward-roger"},
\code{"residual"}, \code{"asymptotic"}. \code{"auto"} gives the exact residual df when
nothing was integrated out, Satterthwaite for a gaussian mixed model, and
\code{Inf} otherwise.}

\item{h}{Step size for the numerical derivatives.}
}
\value{
A list with \code{df}, the \code{method} actually used, and for
Kenward-Roger the adjusted covariance \code{V} and \code{scale}, the factor by which
the Wald F formed with \code{V} is multiplied before it is referred to
F(q, df).
}
\description{
Turns a Wald statistic into an F or t test by working out how much the
estimated standard error is itself worth trusting. Without this a mixed
model reports a chi-square, which treats the variance components as known
and is anti-conservative when there are few clusters.
}
\section{Which method}{

\code{"satterthwaite"} matches a scaled chi-square to the estimated variance of
the contrast. It is the default because it costs first derivatives only, it
applies to every structure this package fits, and it is what \code{lmerTest}
reports.

\code{"kenward-roger"} also inflates the covariance for the uncertainty in the
variance components, which matters most when clusters are few, and gives
an F test its own denominator df and scale factor (Kenward and Roger 1997).
It is computed as \code{pbkrtest} computes it, and agrees with it. It needs a
REML fit (\code{reml = TRUE}) of a gaussian model whose random terms are
intercepts and slopes -- correlated or not, nested or crossed -- with one
residual variance and no weights, and it forms the covariance of all the
observations, so it stops above 4000 rows. Asked for elsewhere it stops and
says why. For one contrast its scale is exactly 1: the t statistic is the
estimate over its standard error from the adjusted covariance \code{V}, on the
df returned.

\code{"residual"} is \code{N - p}, which is exact with nothing integrated out and
optimistic otherwise. \code{"asymptotic"} returns \code{Inf}, recovering the
chi-square.
}

\section{What it is not for}{

Both approximations are derived for LINEAR mixed models. For a non-gaussian
family the small-sample problem is a different one and neither answers it;
\code{\link[=ilm_pb_lrt]{ilm_pb_lrt()}} simulates the null rather than approximating its reference,
and is the remedy there.
}

\examples{
set.seed(1)
d <- ilm_sim(n_id = 25)
f <- ilm_model(score ~ income + (1 | id), data = d, family = "gaussian",
               verbose = FALSE)
L <- c(0, 1)                      # the slope on income
ilm_denom_df(f, L)
}
\references{
Kenward, M. G., & Roger, J. H. (1997). Small sample inference for fixed
effects from restricted maximum likelihood. \emph{Biometrics}, 53(3), 983--997.

Halekoh, U., & Hojsgaard, S. (2014). A Kenward-Roger approximation and
parametric bootstrap methods for tests in linear mixed models: the R
package pbkrtest. \emph{Journal of Statistical Software}, 59(9), 1--30.
}
\seealso{
\code{\link[=ilm_anova]{ilm_anova()}}, \code{\link[=ilm_emmeans]{ilm_emmeans()}}, \code{\link[=ilm_pb_lrt]{ilm_pb_lrt()}}.
}
