% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_check.R, R/zzz-iml-aliases.R
\name{ilm_check_dispersion}
\alias{ilm_check_dispersion}
\alias{iml_check_dispersion}
\title{Is the response more variable than the model allows?}
\usage{
ilm_check_dispersion(object, B = 200L, seed = 1L)
}
\arguments{
\item{object}{A fitted \code{"ilm_model"} object.}

\item{B}{Simulated datasets. A simulated p-value cannot fall below
\code{1 / (B + 1)}, so B must exceed 100 for the strongest verdict to be
reachable at all.}

\item{seed}{Random seed.}
}
\value{
Invisibly, a list with the observed ratio, the simulated
distribution, a p-value and a \code{status}.
}
\description{
The Pearson statistic divided by its degrees of freedom, compared against
the same quantity computed on data simulated from the fitted model. The
reference distribution is simulated rather than assumed, so the test does
not rely on the chi-square approximation that fails for sparse counts.
}
\examples{
set.seed(1)
d <- ilm_sim()
f <- ilm_model(claims ~ income + (1 | id), data = d, family = "poisson",
               verbose = FALSE)
ilm_check_dispersion(f, B = 200)
}
\seealso{
\code{\link[=ilm_check_zeros]{ilm_check_zeros()}} when the excess variance is concentrated at
zero; \code{\link[=ilm_remedies]{ilm_remedies()}}, given this result as \code{dispersion}, writes the
remedy out as the change to make.
}
