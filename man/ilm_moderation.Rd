% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_moderation.R
\name{ilm_moderation}
\alias{ilm_moderation}
\title{Does the effect of X depend on something else?}
\usage{
ilm_moderation(
  object,
  x = NULL,
  moderators = NULL,
  adjust = "holm",
  split = FALSE,
  data = NULL,
  n_keep = 3L,
  seed = 1L,
  progress = NULL
)
}
\arguments{
\item{object}{A fitted \code{\link[=ilm_model]{ilm_model()}}, or an \code{\link[=ilm_dag_model]{ilm_dag_model()}}.}

\item{x}{The treatment, exposure or intervention whose effect might be
moderated. Required, \strong{except} for an \code{\link[=ilm_dag_model]{ilm_dag_model()}}, where the graph
has already named the exposure. There is no sensible default otherwise:
\code{x:m} is the same term whichever of the two you call the moderator, and
only the interpretation distinguishes them.}

\item{moderators}{Candidate moderators. Defaults to the model's other
fixed-effect terms -- variables you already judged worth adjusting for --
which keeps the multiplicity burden honest. Naming them explicitly can
reach anything in the model frame, or in \code{data}.}

\item{adjust}{Passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}; any method it supports,
including \code{"holm"} (the default), \code{"BH"}, \code{"bonferroni"}, \code{"BY"},
\code{"hochberg"}, \code{"hommel"} and \code{"none"}.}

\item{split}{Use an honest sample split instead of an adjustment. See above:
better calibrated, roughly half the power.}

\item{data}{Needed only for a moderator that is not in the model frame.}

\item{n_keep}{Refits to retain on the result, strongest first, so
\code{\link[=ilm_plot_moderation]{ilm_plot_moderation()}} can draw them without refitting. The rest are
refitted on demand.}

\item{seed}{Random seed, used only when \code{split = TRUE}.}

\item{progress}{Show a progress bar. Defaults to \code{\link[=interactive]{interactive()}}.}
}
\value{
An object of class \code{"ilm_moderation"}: a data frame with one row per
candidate (\code{moderator}, \code{df}, \code{statistic}, \code{p}, \code{p_adj}), carrying the
retained refits and the exposure.
}
\description{
Searches a set of candidate moderators for evidence that the effect of a
treatment or exposure varies, testing each by the joint test of its
interaction block and adjusting for having looked at several.
}
\section{What this is for, and what it is not}{


This is the \strong{exploratory} tool. A moderator you hypothesised a priori
belongs in the model, where \code{\link[=ilm_anova]{ilm_anova()}} tests it and \code{\link[=ilm_emmeans]{ilm_emmeans()}} and
\code{\link[=ilm_contrast]{ilm_contrast()}} describe it -- and it owes no multiplicity penalty, because
you did not go looking. Any such interaction already present in \code{object} is
therefore excluded from the search and said so, rather than being charged
for a search it was not part of.
}

\section{Why the adjustment is not optional}{


Trying several moderators and reporting the strongest is a search. With six
candidates and no moderation present at all, that procedure rejected 29.3\%
of the time at a nominal 5\%. Every adjustment method brings it back: Holm,
BH and Bonferroni all landed at 0.027 in the same simulation.
}

\section{Holm, or an honest split}{


\code{split = TRUE} picks the strongest moderator on half the data and tests it
on the other half, which needs no adjustment because the test half was never
used to choose. It is better calibrated -- 0.053 against Holm's conservative
0.027 -- and costs about half the power (0.320 against 0.547 for a numeric
moderator, 0.200 against 0.380 for a three-level factor). The gap widens
with degrees of freedom, so splitting is weakest exactly where experimental
designs live. Holm is the default for that reason.

Splitting is genuinely \emph{required} only when the hypotheses cannot be
counted, as in an open-ended tree search over covariates and split points.
This function searches a named set, so they can be.
}

\examples{
\donttest{
set.seed(1)
d <- data.frame(tx = rbinom(300, 1, 0.5), age = rnorm(300),
                site = factor(sample(c("a", "b", "c"), 300, TRUE)))
d$y <- 0.4 * d$tx + 0.3 * d$age + 0.8 * d$tx * (d$site == "c") + rnorm(300)
fit <- ilm_model(y ~ tx + age + site, data = d, verbose = FALSE)
ilm_moderation(fit, x = "tx")
}
}
\seealso{
\code{\link[=ilm_plot_moderation]{ilm_plot_moderation()}} to see one, \code{\link[=ilm_anova]{ilm_anova()}} for a moderator
you specified in advance, \code{\link[=ilm_trends]{ilm_trends()}} for slopes within levels.
}
