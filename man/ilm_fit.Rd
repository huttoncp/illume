% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_fit.R
\name{ilm_fit}
\alias{ilm_fit}
\title{Fit a multinomial mixed model from a design matrix}
\usage{
ilm_fit(
  X,
  y,
  J = NULL,
  re_list,
  re_struct = NULL,
  ar = NULL,
  ylevels = NULL,
  weights = NULL,
  family = "multinomial",
  verbose = TRUE,
  restarts = 3L,
  joint = FALSE,
  censor = NULL,
  Zd = NULL,
  disp_mu = FALSE
)
}
\arguments{
\item{X}{Numeric design matrix for the fixed effects, with \code{N} rows.}

\item{y}{Integer vector of length \code{N} giving the observed category, coded
\code{1} to \code{J}.}

\item{J}{Integer. Number of outcome categories, for the multinomial family
only; \code{NULL} for every other family.}

\item{re_list}{Named list of random terms. Each element is a grouping vector
(random intercept), a \code{list(group =, Z =)} (random slopes), or a
\code{list(basis =)} (a smooth, from \code{ilm_smooth()}).}

\item{re_struct}{Optional named list of category covariance structures,
parallel to \code{re_list}. Defaults to \code{"us"} for every term. Set \code{d_cor = FALSE} within an element to drop an intercept-slope correlation.}

\item{ar}{Optional AR(1) specification:
\code{list(idx =, n_group =, Tt =)}.}

\item{ylevels}{Optional character vector of category labels, used in output.}

\item{weights}{Optional numeric vector of \strong{frequency} weights: the number of
replicate observations each row stands for, exactly as in a binomial \code{glm()}
fitted to grouped data. A weighted fit is then numerically identical to
expanding each row, standard errors included.

Valid only when the pooled observations share every random effect in the
model -- you may aggregate within subject and covariate pattern, but not
across subjects. These are \strong{not} sampling weights; see the
\code{weights_type} check and the note below.}

\item{family}{Response distribution: a name, or the object returned by
\code{\link[=ilm_family]{ilm_family()}}. See \code{\link[=ilm_family]{ilm_family()}} for what each one assumes.}

\item{verbose}{Logical. Print the checks while fitting.}

\item{restarts}{Integer. Number of optimiser restarts from the previous
solution, which helps on difficult surfaces.}

\item{joint}{Logical. Also compute the joint precision over fixed and random
parameters. Needed by \code{\link[=predict.ilm_model]{predict.ilm_model()}} to propagate uncertainty in penalised
smooth coefficients; \code{\link[=ilm_model]{ilm_model()}} switches it on automatically when the model
contains smooths.}

\item{censor}{Optional censoring specification from \code{\link[=ilm_censor]{ilm_censor()}}, marking
observations known only as an interval -- at or below a floor, at or above
a ceiling. Supported for the gaussian family, where it gives a Tobit model.}

\item{Zd}{Optional design matrix for the dispersion model, one row per
observation. Its columns become a linear predictor for the logarithm of
the dispersion, so its intercept replaces the single dispersion parameter.
Built by \code{\link[=ilm_model]{ilm_model()}} from \code{dispformula}.}

\item{disp_mu}{Logical. Add a term in the logarithm of the fitted mean to
that predictor, making the dispersion a power of the mean.}
}
\value{
An object of class \code{"ilm_model"}: a list whose most useful elements are
\code{checks} (the diagnostic table), \code{Sigma} (fitted category covariances),
\code{beta} (fixed effects as a p-by-C matrix), \code{opt} (optimiser output), \code{sdr}
(\code{TMB::sdreport()} output) and \code{ok} (whether every check passed).
}
\description{
The computational engine. Most users should call \code{\link[=ilm_model]{ilm_model()}} with a formula
instead; this is the entry point when you already have a design matrix, and it
is what the formula interface calls internally.
}
\section{How the model is parameterised}{

With \code{J} outcome categories there are \code{C = J - 1} free linear predictors.
Coefficients use \strong{sum-to-zero} coding, so each one is that category's
deviation from the average across categories, not a contrast against a
baseline. This differs from \code{\link[nnet:multinom]{nnet::multinom()}} and \code{brms}.

Random effects are held as a matrix rather than a long vector, with a
\emph{matrix-normal} prior. Writing it this way means the covariance over groups
and the covariance over categories never have to be combined into one large
Kronecker product, which keeps both memory and computation manageable and
lets AR(1), spatial and i.i.d. structures share a single code path.
}

\section{Choosing a category covariance structure}{

Each random term gets its own structure through \code{re_struct}:
\describe{
\item{\code{"us"}}{unstructured: every variance and correlation free. Costs
\code{C(C+1)/2} parameters, which grows quickly -- 45 at \code{J = 10}.}
\item{\code{"diag"}}{diagonal: categories uncorrelated. Costs \code{C}.}
\item{\code{"rr"} with \code{rank = r}}{reduced rank: \verb{Sigma = Lambda Lambda'} with
\code{Lambda} of size C-by-r. Costs fewer parameters \strong{and} fewer latent
values, which is usually the better trade when a term is stretched.}
}
If a structure is too rich for the data the pre-fit checks will say so and
name a specific rank to try.
}

\section{Models with no random effects}{

\code{re_list} may be empty. The latent parameter block is then omitted entirely
rather than declared with length zero, and for a gaussian response the fit
reports exact t and F inference instead of the large-sample approximations.
See \code{\link[=ilm_model]{ilm_model()}}.
}

\section{Fitting method}{

The random effects are integrated out by the \strong{Laplace approximation},
implemented through \code{RTMB}. Exact quadrature is not an option here: with
crossed random effects the integral does not factorise, so its dimension is
the total number of latent values, often in the thousands. Laplace is an
approximation, and its accuracy depends on having enough data per latent
value -- see the \code{latent_budget} entry in \code{fit$checks}, and
\code{\link[=ilm_consistency]{ilm_consistency()}} to test it directly on your own fit.
}

\section{A warning about sampling weights}{

Passing inverse-probability or survey weights here is not supported and will
mislead you. In a \emph{mixed} model the weight multiplies the conditional
likelihood inside the Laplace integral, so each cluster appears to carry more
information than it does; both the standard errors and the coefficients are
affected. Proper handling needs scaled level-specific weights and a
design-based variance estimator, neither of which is implemented. The
\code{weights_type} check flags weights that look like sampling weights.
}

\references{
Kristensen, K., Nielsen, A., Berg, C. W., Skaug, H., & Bell, B. M. (2016).
TMB: Automatic differentiation and Laplace approximation.
\emph{Journal of Statistical Software}, 70(5), 1--21.

Agresti, A. (2013). \emph{Categorical Data Analysis}, 3rd ed. Wiley.

Pfeffermann, D., Skinner, C. J., Holmes, D. J., Goldstein, H., & Rasbash, J.
(1998). Weighting for unequal selection probabilities in multilevel models.
\emph{Journal of the Royal Statistical Society, Series B}, 60(1), 23--40.

Rabe-Hesketh, S., & Skrondal, A. (2006). Multilevel modelling of complex
survey data. \emph{Journal of the Royal Statistical Society, Series A}, 169(4),
805--827.
}
\seealso{
\code{\link[=ilm_model]{ilm_model()}} for the formula interface, \code{\link[=summary.ilm_model]{summary.ilm_model()}},
\code{\link[=ilm_consistency]{ilm_consistency()}}.
}
