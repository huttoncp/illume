% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilm_plot_model.R, R/zzz-iml-aliases.R
\name{ilm_plot_model}
\alias{ilm_plot_model}
\alias{iml_plot_model}
\title{Diagnostic and summary plots for a fitted model}
\usage{
ilm_plot_model(
  model,
  what = "coef",
  term = NULL,
  conf = 0.95,
  colour = "black",
  fill = "grey70",
  alpha = NULL,
  size = 1,
  main = NULL,
  ...
)
}
\arguments{
\item{model}{A fitted \code{"ilm_model"} object.}

\item{what}{\code{"coef"} for fixed effects with intervals, \code{"effect"} for the
partial effect of one predictor, \code{"random"} for a caterpillar plot of the
estimated group effects, \code{"residual"} to hand off to \code{\link[=ilm_appraise]{ilm_appraise()}}, or
\code{"acf"} to hand off to \code{\link[=ilm_plot_acf]{ilm_plot_acf()}}, which also needs \code{time} and
\code{group}.}

\item{term}{Which predictor or grouping factor to show, for \code{"effect"} and
\code{"random"}.}

\item{conf}{Confidence level for intervals.}

\item{colour, fill, alpha, size}{Appearance.}

\item{main}{Plot title.}

\item{...}{Passed to the underlying plot.}
}
\value{
Invisibly, the data behind the plot.
}
\description{
Diagnostic and summary plots for a fitted model
}
\examples{
set.seed(1)
d <- ilm_sim()
f <- ilm_model(score ~ income + grp + (1 | id), data = d,
               family = "gaussian", verbose = FALSE)
ilm_plot_model(f, "coef")
ilm_plot_model(f, "random")
}
\seealso{
\code{\link[=ilm_appraise]{ilm_appraise()}} for the full residual panel.
}
